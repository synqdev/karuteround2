---
phase: 01-foundation-recording
plan: "05"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .github/workflows/ci.yml
  - .github/CONTRIBUTING.md
autonomous: true

must_haves:
  truths:
    - "CI workflow file exists at .github/workflows/ci.yml"
    - "CI runs lint, type-check, and test jobs on pull requests targeting integration and main branches"
    - "CONTRIBUTING.md documents the integration branch workflow and @claude PR convention"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI pipeline"
      contains: "pull_request"
    - path: ".github/CONTRIBUTING.md"
      provides: "Integration branch workflow documentation"
      contains: "integration"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json scripts"
      via: "npm run lint, npm run type-check, npm test"
      pattern: "npm run"
---

<objective>
Set up GitHub Actions CI/CD with lint, type-check, and test pipeline, plus document the integration branch workflow and @claude tag convention.

Purpose: CI ensures no broken code merges. The @claude convention is the mechanism for Claude to make code changes via PRs. This plan runs Wave 2 parallel with 01-02, 01-04, 01-06.
Output: .github/workflows/ci.yml that runs on PRs. CONTRIBUTING.md documenting the integration branch workflow.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-recording/01-RESEARCH.md
@.planning/phases/01-foundation-recording/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions CI workflow</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <action>
    Create `.github/workflows/ci.yml`:

    ```yaml
    name: CI

    on:
      pull_request:
        branches:
          - integration
          - main

    jobs:
      ci:
        name: Lint, Type-check, Test
        runs-on: ubuntu-latest

        steps:
          - name: Checkout
            uses: actions/checkout@v4

          - name: Setup Node.js
            uses: actions/setup-node@v4
            with:
              node-version: '20'
              cache: 'npm'

          - name: Install dependencies
            run: npm ci

          - name: Lint
            run: npm run lint
            # NOTE: next lint was removed in Next.js 16 — package.json uses "eslint src" directly

          - name: Type-check
            run: npm run type-check
            # tsc --noEmit

          - name: Test
            run: npm test -- --passWithNoTests
            # --passWithNoTests allows CI to pass when no test files exist yet (Phase 8 adds tests)
    ```

    The CI intentionally uses `--passWithNoTests` because test files are added in Phase 8. The three jobs (lint, type-check, test) run sequentially within one job for simplicity — splitting into parallel jobs is premature optimization for a solo project.

    Verify that `package.json` has the required scripts. If `01-01` already added them, confirm they exist:
    - `"lint": "eslint src"` or `"lint": "eslint ."`
    - `"type-check": "tsc --noEmit"`
    - `"test": "jest --passWithNoTests"` (or vitest equivalent)

    If any script is missing, add it to `package.json` now.
  </action>
  <verify>
    Check: `ls .github/workflows/ci.yml` — exists.
    Check: `grep "pull_request" .github/workflows/ci.yml` — found.
    Check: `grep "npm run lint" .github/workflows/ci.yml` — found.
    Check: `grep "npm run type-check" .github/workflows/ci.yml` — found.
    Check: `grep "passWithNoTests" .github/workflows/ci.yml` — found.
    Run locally: `npm run lint && npm run type-check` — both exit 0.
  </verify>
  <done>
    .github/workflows/ci.yml exists and will run lint, type-check, test on PRs to integration and main branches.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration branch workflow documentation</name>
  <files>
    .github/CONTRIBUTING.md
  </files>
  <action>
    Create `.github/CONTRIBUTING.md` documenting the development workflow:

    ```markdown
    # Contributing to Karute

    ## Branch Workflow

    This project uses an integration branch workflow:

    ```
    feature/task-name → integration → main
    ```

    - **Never commit directly to `main` or `integration`**
    - Create a feature branch per task: `git checkout -b feature/01-03-auth`
    - Open a PR targeting `integration` when the task is complete
    - `integration` is merged to `main` at milestone boundaries (end of phase)

    ### Branch Naming

    | Type | Pattern | Example |
    |------|---------|---------|
    | Feature / task | `feature/{phase}-{plan}-{description}` | `feature/01-03-supabase-auth` |
    | Bug fix | `fix/{description}` | `fix/login-redirect` |
    | Hotfix to main | `hotfix/{description}` | `hotfix/env-var-missing` |

    ## @claude Tag Convention

    To request Claude to make code changes via a PR comment:

    1. Open a PR or issue
    2. Add a comment starting with `@claude` followed by your request:

    ```
    @claude Please add error handling to the login form for network timeouts
    ```

    Claude will:
    - Read the PR diff and related files
    - Implement the requested change
    - Push a commit to the PR branch

    **Use @claude for:** bug fixes, enhancements to existing PRs, code review follow-ups.
    **Don't use @claude for:** large features (use `/gsd:execute-phase` instead), architecture changes.

    ## CI

    CI runs automatically on all PRs targeting `integration` or `main`:
    - **Lint**: ESLint via `npm run lint`
    - **Type-check**: TypeScript via `npm run type-check`
    - **Test**: Jest via `npm test`

    All checks must pass before merging. CI uses `--passWithNoTests` until integration tests are added in Phase 8.

    ## Environment Setup

    1. Copy `.env.local.example` to `.env.local`
    2. Fill in Supabase dev project credentials (see `.env.local.example` for where to find them)
    3. Run `npm run dev` — app starts at localhost:3000
    ```
  </action>
  <verify>
    Check: `ls .github/CONTRIBUTING.md` — exists.
    Check: `grep "@claude" .github/CONTRIBUTING.md` — found.
    Check: `grep "integration" .github/CONTRIBUTING.md` — found.
  </verify>
  <done>
    CONTRIBUTING.md documents integration branch workflow, @claude PR convention, and CI checks.
  </done>
</task>

</tasks>

<verification>
1. `.github/workflows/ci.yml` exists
2. CI triggers on PRs to `integration` and `main`
3. CI runs: `npm run lint`, `npm run type-check`, `npm test -- --passWithNoTests`
4. `npm run lint` exits 0 locally
5. `npm run type-check` exits 0 locally
6. `.github/CONTRIBUTING.md` documents integration branch and @claude convention
</verification>

<success_criteria>
- CI workflow triggers on pull_request to integration and main
- Three checks: lint (eslint, not next lint), type-check (tsc --noEmit), test (--passWithNoTests)
- CONTRIBUTING.md has branch naming pattern, @claude usage, and env setup instructions
- All CI steps pass locally before this plan is considered done
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-recording/01-05-SUMMARY.md`
</output>
