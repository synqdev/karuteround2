---
phase: 03-customer-management
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/customers/CustomerTable.tsx
  - src/components/customers/KaruteHistoryList.tsx
  - src/app/[locale]/(app)/customers/[id]/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All customer navigation uses locale-aware routing so URLs include the active locale prefix"
    - "Clicking a customer row in the table navigates to the locale-prefixed customer profile"
    - "Clicking a karute history row navigates to the locale-prefixed karute detail page"
    - "The Back link on the customer profile navigates to the locale-prefixed customer list"
  artifacts:
    - path: "src/components/customers/CustomerTable.tsx"
      provides: "Locale-aware customer row navigation"
      contains: "from '@/i18n/navigation'"
    - path: "src/components/customers/KaruteHistoryList.tsx"
      provides: "Locale-aware karute history navigation"
      contains: "from '@/i18n/navigation'"
    - path: "src/app/[locale]/(app)/customers/[id]/page.tsx"
      provides: "Locale-aware Back link"
      contains: "from '@/i18n/navigation'"
  key_links:
    - from: "src/components/customers/CustomerTable.tsx"
      to: "@/i18n/navigation"
      via: "useRouter import"
      pattern: "import.*useRouter.*from '@/i18n/navigation'"
    - from: "src/components/customers/KaruteHistoryList.tsx"
      to: "@/i18n/navigation"
      via: "useRouter import"
      pattern: "import.*useRouter.*from '@/i18n/navigation'"
    - from: "src/app/[locale]/(app)/customers/[id]/page.tsx"
      to: "@/i18n/navigation"
      via: "Link import"
      pattern: "import.*Link.*from '@/i18n/navigation'"
---

<objective>
Fix locale-aware navigation in all customer management components.

Purpose: Three customer components import `useRouter` from `next/navigation` or `Link` from `next/link` instead of their locale-aware equivalents from `@/i18n/navigation`. This causes navigation to lose the locale prefix (e.g., `/en/customers/...` becomes `/customers/...`), breaking the i18n routing established in Phase 1.

Output: All three files use `@/i18n/navigation` exports, matching the pattern already used in `locale-toggle.tsx` and the sidebar.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/i18n/navigation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace next/navigation imports with @/i18n/navigation in CustomerTable and KaruteHistoryList</name>
  <files>src/components/customers/CustomerTable.tsx, src/components/customers/KaruteHistoryList.tsx</files>
  <action>
In `CustomerTable.tsx` (line 2):
- Change `import { useRouter, useSearchParams, usePathname } from 'next/navigation'`
- To: `import { useRouter, usePathname } from '@/i18n/navigation'` and `import { useSearchParams } from 'next/navigation'`
- Note: `useSearchParams` is NOT exported by `@/i18n/navigation` (createNavigation only exports Link, redirect, usePathname, useRouter), so it must remain imported from `next/navigation`.

In `KaruteHistoryList.tsx` (line 2):
- Change `import { useRouter, useSearchParams, usePathname } from 'next/navigation'`
- To: `import { useRouter, usePathname } from '@/i18n/navigation'` and `import { useSearchParams } from 'next/navigation'`
- Same reasoning: `useSearchParams` stays from `next/navigation`.

No other changes needed in these files. The `router.push()` and `router.replace()` calls already use relative paths (e.g., `/customers/${id}`, `/karute/${id}`) which the locale-aware router will automatically prefix with the current locale.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Then grep to confirm no remaining `useRouter` or `usePathname` imports from `next/navigation` in these two files:
```
grep -n "from 'next/navigation'" src/components/customers/CustomerTable.tsx src/components/customers/KaruteHistoryList.tsx
```
Expected: only `useSearchParams` remains imported from `next/navigation`.
  </verify>
  <done>CustomerTable.tsx and KaruteHistoryList.tsx import useRouter and usePathname from @/i18n/navigation, with only useSearchParams remaining from next/navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Replace next/link with @/i18n/navigation Link in customer profile page</name>
  <files>src/app/[locale]/(app)/customers/[id]/page.tsx</files>
  <action>
In `page.tsx` (line 2):
- Change `import Link from 'next/link'`
- To: `import { Link } from '@/i18n/navigation'`
- Note: The `@/i18n/navigation` Link is a named export (not default), so the import syntax changes.

The `href="/customers"` on line 65 stays the same — the locale-aware Link component will automatically prefix it with the current locale.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Then grep:
```
grep -n "from 'next/link'" src/app/[locale]/(app)/customers/[id]/page.tsx
```
Expected: no matches (import replaced).
  </verify>
  <done>Customer profile page uses Link from @/i18n/navigation. The Back button href="/customers" is automatically locale-prefixed by the i18n Link component.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` passes with no errors
2. No customer component imports `useRouter` or `usePathname` from `next/navigation`
3. No customer component imports `Link` from `next/link`
4. All navigation imports come from `@/i18n/navigation` (except `useSearchParams` which is correctly from `next/navigation`)
</verification>

<success_criteria>
All three customer management files use locale-aware navigation from `@/i18n/navigation`, ensuring URLs include the active locale prefix. The gap identified in 03-VERIFICATION.md is fully closed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-customer-management/03-04-SUMMARY.md`
</output>
