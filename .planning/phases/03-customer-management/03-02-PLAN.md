---
phase: 03-customer-management
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/(app)/customers/page.tsx
  - src/app/(app)/customers/loading.tsx
  - src/components/customers/CustomerTable.tsx
  - src/components/customers/CustomerSearch.tsx
  - src/components/customers/CustomerForm.tsx
  - src/components/customers/CustomerSheet.tsx
  - src/components/customers/CustomerEmptyState.tsx
  - src/components/customers/Pagination.tsx
autonomous: true

must_haves:
  truths:
    - "User can view a paginated table of customers with sortable columns (name, contact, last visit, visit count)"
    - "User can search customers by typing in a debounced search input that filters by name, furigana, phone, and email"
    - "User can click '+ New Customer' to open a right-side sheet with a creation form"
    - "After creating a customer, the sheet closes, the list refreshes, and a success toast appears"
    - "User can click a table row to navigate to that customer's profile page"
    - "Each customer row shows an initials avatar with a deterministic pastel color"
    - "Page shows 10 customers per page with page number navigation at bottom"
  artifacts:
    - path: "src/app/(app)/customers/page.tsx"
      provides: "Server Component that reads searchParams (query, page, sort) and renders the customer list"
      contains: "await searchParams"
    - path: "src/components/customers/CustomerTable.tsx"
      provides: "Client Component with sortable columns, hover highlight, row click navigation"
      contains: "use client"
    - path: "src/components/customers/CustomerSearch.tsx"
      provides: "Client Component with debounced search input updating URL params"
      contains: "useDebouncedCallback"
    - path: "src/components/customers/CustomerSheet.tsx"
      provides: "Sheet wrapper that opens from right with CustomerForm inside"
      contains: "Sheet"
    - path: "src/components/customers/CustomerForm.tsx"
      provides: "Form with name, furigana, phone, email fields using react-hook-form + zod"
      contains: "useForm"
    - path: "src/components/customers/Pagination.tsx"
      provides: "Page number navigation component using URL params"
      contains: "page"
  key_links:
    - from: "src/app/(app)/customers/page.tsx"
      to: "src/lib/customers/queries.ts"
      via: "listCustomers() call in Server Component"
      pattern: "listCustomers"
    - from: "src/components/customers/CustomerSearch.tsx"
      to: "URL search params"
      via: "useRouter().replace() with debounced query param"
      pattern: "replace.*pathname"
    - from: "src/components/customers/CustomerForm.tsx"
      to: "src/actions/customers.ts"
      via: "createCustomer Server Action on submit"
      pattern: "createCustomer"
    - from: "src/components/customers/CustomerTable.tsx"
      to: "/customers/[id]"
      via: "router.push on row click"
      pattern: "router\\.push.*customers"
---

<objective>
Build the customer list page with searchable/sortable table, pagination, and a right-side sheet for creating new customers.

Purpose: This is the primary customer management interface — CUST-01 (create) and CUST-03 (list/search) are fulfilled here.
Output: Customers list page at /customers with working search, sort, pagination, and new customer creation via sheet.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-customer-management/03-RESEARCH.md
@.planning/phases/03-customer-management/03-CONTEXT.md
@.planning/phases/03-customer-management/03-01-SUMMARY.md
@src/actions/customers.ts
@src/lib/customers/queries.ts
@src/lib/customers/utils.ts
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Customer list page with search and pagination</name>
  <files>
    src/app/(app)/customers/page.tsx
    src/app/(app)/customers/loading.tsx
    src/components/customers/CustomerSearch.tsx
    src/components/customers/CustomerEmptyState.tsx
    src/components/customers/Pagination.tsx
  </files>
  <action>
    **Install use-debounce** if not already installed:
    ```bash
    npm install use-debounce
    ```

    **Add shadcn components** if not already added:
    ```bash
    npx shadcn@latest add table sheet form input button badge avatar toast
    ```
    (Skip any that already exist — the CLI will warn.)

    **Create `src/app/(app)/customers/page.tsx`** — Server Component:
    - Accept `searchParams: Promise<{ query?: string; page?: string; sort?: string; order?: string }>` and await it (Next.js 16 pattern — CRITICAL: must await)
    - Extract `query`, `page` (default '1'), `sort` (default 'updated_at'), `order` (default 'desc')
    - Call `listCustomers({ query, page: Number(page), pageSize: 10, sortBy: sort, sortOrder: order })` from `@/lib/customers/queries`
    - Use `useTranslations('customers')` via `getTranslations('customers')` for server component (next-intl server pattern)
    - Render page layout:
      - Page header: `<h1>` with translated "Customers" title + `<CustomerSheet />` containing "+ New Customer" button (positioned top-right)
      - `<CustomerSearch />` below header
      - Wrap table in `<Suspense key={query + page} fallback={<CustomerTableSkeleton />}>` to prevent stale flash on search (per research pitfall 5)
      - `<CustomerTable customers={customers} />` — pass the fetched customer array
      - `<Pagination currentPage={Number(page)} totalPages={totalPages} />` at bottom
      - If no customers exist at all (totalCount === 0 and no query), show `<CustomerEmptyState />` instead of table

    **Create `src/app/(app)/customers/loading.tsx`** — skeleton loading state:
    - Render a skeleton that mimics the page layout: fake header, search bar skeleton, 5 table row skeletons with placeholder rectangles
    - Use Tailwind `animate-pulse` with `bg-muted` rectangles

    **Create `src/components/customers/CustomerSearch.tsx`** — Client Component:
    - `'use client'` directive
    - Use `useDebouncedCallback` from `use-debounce` with 300ms delay
    - Read current query from `useSearchParams()`
    - On input change: update URL `?query=` param via `useRouter().replace()`, reset `?page=` to 1
    - On clear/empty: remove `?query=` param
    - Render shadcn `<Input>` with search icon (Lucide `Search`), set `defaultValue` from current search param
    - Use translated placeholder from `useTranslations('customers')`

    **Create `src/components/customers/CustomerEmptyState.tsx`**:
    - Welcoming empty state per user's specific idea: icon (Lucide `Users` or `UserPlus`) + translated title + description + CTA that triggers the new customer sheet
    - Not just empty text — should feel inviting for first-time experience

    **Create `src/components/customers/Pagination.tsx`** — Client Component:
    - `'use client'` directive
    - Accept `currentPage` and `totalPages` props
    - Render page number links (not buttons — use `<Link>` or `useRouter` for URL-based navigation)
    - Each page link updates `?page=N` while preserving existing `?query=` param
    - Show "Previous" / "Next" + individual page numbers
    - Highlight current page
    - Hide pagination entirely if totalPages <= 1
  </action>
  <verify>
    `ls src/app/\\(app\\)/customers/page.tsx` — file exists.
    `grep "await searchParams" src/app/\\(app\\)/customers/page.tsx` — found (Next.js 16 async pattern).
    `grep "listCustomers" src/app/\\(app\\)/customers/page.tsx` — found.
    `grep "useDebouncedCallback" src/components/customers/CustomerSearch.tsx` — found.
    `grep "totalPages" src/components/customers/Pagination.tsx` — found.
  </verify>
  <done>
    Customer list page renders at /customers, reads search/page/sort from URL params, fetches from Supabase via listCustomers, shows search input with 300ms debounce, paginated with 10 per page, shows empty state when no customers exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Customer table with sorting and new customer sheet</name>
  <files>
    src/components/customers/CustomerTable.tsx
    src/components/customers/CustomerForm.tsx
    src/components/customers/CustomerSheet.tsx
  </files>
  <action>
    **Create `src/components/customers/CustomerTable.tsx`** — Client Component:
    - `'use client'` directive
    - Accept `customers: Customer[]` prop
    - Render shadcn `<Table>` with columns (per user decision):
      1. **Name** — initials avatar circle (using `getAvatarColor(customer.id)` and `getInitials(customer.name)`) next to customer name. Show furigana in smaller text below name if present.
      2. **Contact** — show phone and/or email, whichever exists. Format as two lines if both present.
      3. **Last Visit** — show `updated_at` formatted as date (this is a proxy until karute_records exist; Phase 4 will replace with actual last session date). Show "-" if no visits.
      4. **Visits** — show "0" for now (placeholder until karute_records join query in Phase 4).
    - Column headers are clickable for sorting — on click, update URL `?sort=` and `?order=` params (toggle asc/desc). Show sort indicator arrow on active column.
    - Subtle row highlight on hover: `hover:bg-muted/50` (per user decision: subtle highlight, NO pointer cursor change — do NOT add `cursor-pointer`)
    - Click row navigates to `/customers/[id]` via `useRouter().push()`
    - Use `useTranslations('customers')` for all column header text

    **Create `src/components/customers/CustomerSheet.tsx`** — Client Component:
    - `'use client'` directive
    - Uses shadcn `<Sheet>` component that slides from the right (per user decision: "Sheet/drawer slides in from the right side")
    - Trigger button: "+ New Customer" styled as primary button
    - Sheet contains `<CustomerForm />` for creation
    - On successful form submission (form calls `createCustomer` and gets `{ success: true }`):
      1. Close the sheet (set open state to false)
      2. Show success toast using sonner or shadcn toast: translated "Customer created" message
      3. List auto-refreshes because createCustomer calls revalidatePath('/customers')
    - Pass an `onSuccess` callback to CustomerForm that handles close + toast

    **Create `src/components/customers/CustomerForm.tsx`** — Client Component:
    - `'use client'` directive
    - Accept optional props: `customerId?: string`, `defaultValues?: Partial<CustomerFormValues>`, `onSuccess?: () => void`, `onCancel?: () => void`
    - Use `useForm` with `zodResolver` and the CustomerFormSchema:
      - `name`: required, min 1, max 100
      - `furigana`: optional, max 100
      - `phone`: optional, max 20
      - `email`: optional, valid email or empty string
    - Fields (per user decision):
      1. Name (required) — `<Input>` with translated label and placeholder
      2. Furigana/Reading (optional) — `<Input>` with translated label, placeholder "たなか たろう"
      3. Phone (optional) — `<Input type="tel">` with translated label
      4. Email (optional) — `<Input type="email">` with translated label
    - On submit:
      - If `customerId` provided: call `updateCustomer(customerId, values)`
      - Else: call `createCustomer(values)`
      - If result has `duplicateWarning`: show warning toast (per user decision: "warn but allow saving") — the customer was still created
      - If `success: true`: call `onSuccess?.()`
      - If `success: false`: show error toast with `result.error`
    - Show "Saving..." state on submit button while `isSubmitting`
    - Cancel button calls `onCancel?.()` and resets form
    - Use `useTranslations('customers')` for all labels
  </action>
  <verify>
    `grep "use client" src/components/customers/CustomerTable.tsx` — found.
    `grep "getAvatarColor" src/components/customers/CustomerTable.tsx` — found.
    `grep "hover:bg-muted" src/components/customers/CustomerTable.tsx` — found.
    `grep "Sheet" src/components/customers/CustomerSheet.tsx` — found.
    `grep "side.*right" src/components/customers/CustomerSheet.tsx` — found (sheet from right).
    `grep "useForm" src/components/customers/CustomerForm.tsx` — found.
    `grep "createCustomer" src/components/customers/CustomerForm.tsx` — found.
    `grep "duplicateWarning" src/components/customers/CustomerForm.tsx` — found.
  </verify>
  <done>
    Customer table renders with 4 sortable columns, initials avatars with deterministic colors, hover highlight (no cursor change), row click navigation. Sheet slides from right with creation form. Form validates with Zod, handles duplicate warnings, shows toast on success/error.
  </done>
</task>

</tasks>

<verification>
1. `/customers` page renders with search input, table, and pagination
2. Search input debounces at 300ms and updates URL `?query=` param
3. Table shows 4 columns: Name (with avatar), Contact, Last Visit, Visits
4. Column headers clickable to sort (updates URL params)
5. Rows highlight on hover (subtle, no cursor change)
6. Clicking a row navigates to `/customers/[id]`
7. "+ New Customer" button opens sheet from right
8. Sheet form has 4 fields: name (required), furigana, phone, email
9. After save: sheet closes, list refreshes, success toast shown
10. Duplicate name shows warning toast but saves anyway
11. Empty state shown when no customers exist (welcoming design with CTA)
12. Pagination shows page numbers, 10 per page, hidden when 1 page
13. All text uses i18n translations (no hardcoded EN strings)
</verification>

<success_criteria>
- CUST-01 fulfilled: user can create customer with name and optional contact info via sheet
- CUST-03 fulfilled: user can view paginated/searchable customer list and find customer by name (and furigana, phone, email)
- All user-locked decisions implemented: table layout, sortable columns, initials avatar, debounced search, 10 per page, sheet from right, duplicate warning, row hover highlight, no cursor change
- No deferred features present: no delete button, no tags, no photo upload
</success_criteria>

<output>
After completion, create `.planning/phases/03-customer-management/03-02-SUMMARY.md`
</output>
