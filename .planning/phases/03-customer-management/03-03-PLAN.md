---
phase: 03-customer-management
plan: "03"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/(app)/customers/[id]/page.tsx
  - src/app/(app)/customers/[id]/loading.tsx
  - src/components/customers/CustomerProfileHeader.tsx
  - src/components/customers/CustomerInlineEdit.tsx
  - src/components/customers/KaruteHistoryList.tsx
autonomous: true

must_haves:
  truths:
    - "User can view a customer's profile with name, contact info, visit stats, and initials avatar"
    - "User can click Edit to toggle inline editing of name, furigana, phone, and email with Save/Cancel"
    - "User can see a karute history list in reverse-chronological order (empty state in Phase 3, populated in Phase 4)"
    - "Back button navigates to /customers"
    - "Karute history is paginated with 10 per page"
  artifacts:
    - path: "src/app/(app)/customers/[id]/page.tsx"
      provides: "Server Component that fetches customer + karute_records in parallel"
      contains: "await params"
    - path: "src/components/customers/CustomerProfileHeader.tsx"
      provides: "Header with avatar, name, contact info, visit stats, Edit button"
      contains: "getAvatarColor"
    - path: "src/components/customers/CustomerInlineEdit.tsx"
      provides: "Inline editing form for customer fields with Save/Cancel"
      contains: "updateCustomer"
    - path: "src/components/customers/KaruteHistoryList.tsx"
      provides: "Karute session history list with date, staff name, summary snippet"
      contains: "karute"
  key_links:
    - from: "src/app/(app)/customers/[id]/page.tsx"
      to: "src/lib/customers/queries.ts"
      via: "getCustomer(id) call"
      pattern: "getCustomer"
    - from: "src/app/(app)/customers/[id]/page.tsx"
      to: "supabase karute_records query"
      via: "Promise.all parallel fetch"
      pattern: "Promise\\.all"
    - from: "src/components/customers/CustomerInlineEdit.tsx"
      to: "src/actions/customers.ts"
      via: "updateCustomer Server Action on save"
      pattern: "updateCustomer"
    - from: "src/components/customers/KaruteHistoryList.tsx"
      to: "/karute/[id]"
      via: "row click navigation"
      pattern: "karute"
---

<objective>
Build the customer profile page with header info, inline editing, and karute history list.

Purpose: This fulfills CUST-02 (edit customer) and CUST-04 (view karute history). The karute history list scaffolds the query now so Phase 4 just adds data without modifying this page.
Output: Customer profile page at /customers/[id] with inline editing and karute history (empty state until Phase 4).
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-customer-management/03-RESEARCH.md
@.planning/phases/03-customer-management/03-CONTEXT.md
@.planning/phases/03-customer-management/03-01-SUMMARY.md
@src/actions/customers.ts
@src/lib/customers/queries.ts
@src/lib/customers/utils.ts
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Customer profile page with parallel data fetching</name>
  <files>
    src/app/(app)/customers/[id]/page.tsx
    src/app/(app)/customers/[id]/loading.tsx
  </files>
  <action>
    **Create `src/app/(app)/customers/[id]/page.tsx`** — Server Component:
    - Accept `params: Promise<{ id: string }>` and `searchParams: Promise<{ historyPage?: string }>` — await both (Next.js 16 pattern)
    - Extract `id` from params, `historyPage` (default '1') from searchParams
    - Fetch customer and karute records in parallel using `Promise.all`:
      ```typescript
      const supabase = await createClient()
      const [customerResult, karuteResult] = await Promise.all([
        supabase.from('customers').select('*').eq('id', id).single(),
        supabase
          .from('karute_records')
          .select('id, created_at, summary, staff_profile_id, session_date', { count: 'exact' })
          .eq('client_id', id)
          .order('session_date', { ascending: false })
          .range((historyPage - 1) * 10, historyPage * 10 - 1),
      ])
      ```
    - If customer not found, call `notFound()` from `next/navigation`
    - Compute visit stats from karuteResult: `visitCount = karuteResult.count ?? 0`, `lastVisit = karuteResult.data?.[0]?.session_date ?? null`
    - Use `getTranslations('customers')` for server-side i18n
    - Render layout:
      1. Back link at top: `<Link href="/customers">` with left arrow icon + translated "Back"
      2. `<CustomerProfileHeader>` with customer data, visitCount, lastVisit
      3. `<KaruteHistoryList>` with karute records array, pagination props
    - Note: The `client_id` column in `karute_records` references `customers.id` — use this for the join

    **Create `src/app/(app)/customers/[id]/loading.tsx`** — skeleton:
    - Large avatar circle skeleton + name/contact text skeletons in header area
    - 3 list item skeletons for karute history
    - Use Tailwind `animate-pulse` with `bg-muted`
  </action>
  <verify>
    `ls src/app/\\(app\\)/customers/\\[id\\]/page.tsx` — file exists.
    `grep "await params" src/app/\\(app\\)/customers/\\[id\\]/page.tsx` — found (Next.js 16 async params).
    `grep "Promise.all" src/app/\\(app\\)/customers/\\[id\\]/page.tsx` — found (parallel fetch).
    `grep "notFound" src/app/\\(app\\)/customers/\\[id\\]/page.tsx` — found (404 handling).
    `grep "client_id" src/app/\\(app\\)/customers/\\[id\\]/page.tsx` — found (correct FK column).
  </verify>
  <done>
    Profile page fetches customer + karute records in parallel, shows 404 on missing customer, computes visit stats, renders header + history list. Loading skeleton displays while fetching.
  </done>
</task>

<task type="auto">
  <name>Task 2: Profile header with inline edit and karute history list</name>
  <files>
    src/components/customers/CustomerProfileHeader.tsx
    src/components/customers/CustomerInlineEdit.tsx
    src/components/customers/KaruteHistoryList.tsx
  </files>
  <action>
    **Create `src/components/customers/CustomerProfileHeader.tsx`** — Client Component:
    - `'use client'` directive
    - Accept props: `customer: Customer`, `visitCount: number`, `lastVisit: string | null`
    - Render header layout (per user decision):
      1. **Large initials avatar** — bigger than table version (~64px), using `getAvatarColor(customer.id)` and `getInitials(customer.name)`, displayed as a circle
      2. **Customer name** — large text, with furigana below in smaller muted text if present
      3. **Contact info** — phone and email displayed with icons (Lucide `Phone`, `Mail`), show "-" if not provided
      4. **Visit stats** — "{count} visits" and "Last visit: {date}" using translated strings with interpolation. Format date in locale-appropriate format. Show "No visits yet" if lastVisit is null.
      5. **Edit button** — shadcn `<Button variant="outline">` with translated "Edit" text
    - When Edit is clicked, toggle to `<CustomerInlineEdit>` component (swap the display fields for editable form fields in-place)
    - Use `useTranslations('customers')` for all text
    - When inline edit saves or cancels, switch back to display mode

    **Create `src/components/customers/CustomerInlineEdit.tsx`** — Client Component:
    - `'use client'` directive
    - Accept props: `customer: Customer`, `onSave: () => void`, `onCancel: () => void`
    - Use `useForm` with `zodResolver` and same CustomerFormSchema as CustomerForm (DRY — import the schema from a shared location, or define inline with same shape)
    - Pre-populate `defaultValues` from customer data
    - Render fields inline (replacing the display-only header fields):
      - Name input (required)
      - Furigana input (optional)
      - Phone input (optional)
      - Email input (optional)
    - Save button calls `updateCustomer(customer.id, values)` Server Action
    - On success: show toast "Customer updated", call `onSave()` to switch back to display mode. The page auto-refreshes because updateCustomer calls revalidatePath.
    - On error: show error toast, stay in edit mode
    - Cancel button: call `onCancel()` to switch back to display mode without saving
    - Save and Cancel buttons side by side below the fields

    **Create `src/components/customers/KaruteHistoryList.tsx`** — component (can be Server or Client):
    - Accept props: `records: KaruteRecord[]` (or a subset type with id, session_date, summary, staff_profile_id), `currentPage: number`, `totalPages: number`
    - Section header: translated "Session History" heading
    - If records is empty: show welcoming empty state — icon (Lucide `FileText` or `ClipboardList`) + translated "No visits yet" message. Do NOT show this as broken — it's expected in Phase 3 before any karute records exist.
    - If records exist: render a list of session rows (per user decision):
      - Each row shows: **date** (formatted session_date), **staff member name** (staff_profile_id lookup — for Phase 3, show "Staff" placeholder since profiles join isn't built yet), **AI summary snippet** (~100 chars, truncated with ellipsis)
      - Click row navigates to `/karute/[id]` (per user decision)
      - Subtle hover highlight matching table style
    - Pagination at bottom using same `<Pagination>` component from 03-02, but with `historyPage` URL param instead of `page`
    - Use `useTranslations('customers')` for headings and empty state text
  </action>
  <verify>
    `grep "getAvatarColor" src/components/customers/CustomerProfileHeader.tsx` — found.
    `grep "getInitials" src/components/customers/CustomerProfileHeader.tsx` — found.
    `grep "updateCustomer" src/components/customers/CustomerInlineEdit.tsx` — found.
    `grep "useForm" src/components/customers/CustomerInlineEdit.tsx` — found.
    `grep "session_date\\|karute" src/components/customers/KaruteHistoryList.tsx` — found.
    `grep "empty\\|No visits" src/components/customers/KaruteHistoryList.tsx` — found (empty state).
  </verify>
  <done>
    Profile header displays customer info with large avatar, contact details, visit stats, and Edit button. Inline edit replaces display fields with form inputs, saves via updateCustomer, shows toast. Karute history list renders sessions in reverse-chronological order with empty state for Phase 3. Session rows link to /karute/[id].
  </done>
</task>

</tasks>

<verification>
1. `/customers/[id]` page fetches customer + karute records in parallel via Promise.all
2. 404 page shown for invalid customer ID
3. Profile header shows large avatar, name, furigana, contact info, visit stats
4. Edit button toggles inline editing with name, furigana, phone, email fields
5. Save updates customer via updateCustomer, shows toast, switches back to display
6. Cancel switches back to display without saving
7. Karute history section shows "No visits yet" empty state (expected for Phase 3)
8. Session rows show date, staff name placeholder, summary snippet (~100 chars)
9. Session row click navigates to /karute/[id]
10. History is paginated with 10 per page via `?historyPage=` URL param
11. Back link navigates to /customers
12. All text uses i18n translations
</verification>

<success_criteria>
- CUST-02 fulfilled: user can edit customer name and contact info via inline editing
- CUST-04 fulfilled: user can view karute history in reverse-chronological order (empty until Phase 4 adds data, but the query is scaffolded)
- All user-locked decisions implemented: header with avatar + stats + Edit, inline editing with Save/Cancel, history with date + staff + summary snippet, session row navigation to /karute/[id], 10 per page pagination, Back to /customers
- No deferred features present: no delete, no tags, no photo upload
- Phase 4 does NOT need to modify this page — karute_records query is already wired
</success_criteria>

<output>
After completion, create `.planning/phases/03-customer-management/03-03-SUMMARY.md`
</output>
