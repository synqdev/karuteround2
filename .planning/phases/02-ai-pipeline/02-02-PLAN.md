---
phase: 02-ai-pipeline
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/api/ai/extract/route.ts
  - src/app/api/ai/summarize/route.ts
autonomous: true

must_haves:
  truths:
    - "POST /api/ai/extract accepts { transcript, locale } and returns a schema-valid entries array"
    - "POST /api/ai/summarize accepts { transcript, locale } and returns a prose summary string"
    - "Each entry has category (from predefined set), title, source_quote, and confidence_score (0-1)"
    - "Entry titles and summary text are in the language matching the locale parameter"
    - "Source quotes preserve the original spoken language regardless of locale"
  artifacts:
    - path: "src/app/api/ai/extract/route.ts"
      provides: "GPT structured extraction endpoint"
      exports: ["POST", "maxDuration"]
    - path: "src/app/api/ai/summarize/route.ts"
      provides: "GPT session summary endpoint"
      exports: ["POST", "maxDuration"]
  key_links:
    - from: "src/app/api/ai/extract/route.ts"
      to: "src/types/ai.ts"
      via: "imports ExtractionResultSchema for zodResponseFormat"
      pattern: "import.*ExtractionResultSchema.*from.*types/ai"
    - from: "src/app/api/ai/extract/route.ts"
      to: "src/lib/prompts.ts"
      via: "imports getExtractionSystemPrompt"
      pattern: "import.*getExtractionSystemPrompt.*from.*lib/prompts"
    - from: "src/app/api/ai/summarize/route.ts"
      to: "src/lib/prompts.ts"
      via: "imports getSummarySystemPrompt"
      pattern: "import.*getSummarySystemPrompt.*from.*lib/prompts"
---

<objective>
Create the GPT extraction and summary API routes that transform a transcript into structured entries and a prose summary.

Purpose: These two endpoints complete the AI backend — after Plan 01's transcription route produces text, these routes extract structured data and generate a readable summary. They run in parallel (called via Promise.all in Plan 03).
Output: Two working API endpoints ready for pipeline orchestration.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ai-pipeline/02-RESEARCH.md
@.planning/phases/02-ai-pipeline/02-CONTEXT.md
@.planning/phases/02-ai-pipeline/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GPT structured extraction API route</name>
  <files>src/app/api/ai/extract/route.ts</files>
  <action>
Create `src/app/api/ai/extract/route.ts`:
- Export `maxDuration = 60`
- Export async `POST` handler that:
  1. Parses JSON body: `{ transcript: string, locale: string }`
  2. Validates `transcript` exists and is non-empty — return 400 if missing
  3. Gets system prompt via `getExtractionSystemPrompt(locale)` from `@/lib/prompts`
  4. Calls `openai.chat.completions.parse()` with:
     - `model: 'gpt-4o-mini'`
     - System message with the locale-aware prompt
     - User message: `"Extract karute entries from this session transcript:\n\n${transcript}"`
     - `response_format: zodResponseFormat(ExtractionResultSchema, 'extraction_result')`
  5. Extracts parsed result: `completion.choices[0].message.parsed`
  6. Returns `NextResponse.json(result)` — the `{ entries: Entry[] }` object
  7. Wraps in try/catch — on error, return 500 `{ error: 'Extraction failed' }`

Import `zodResponseFormat` from `openai/helpers/zod`.
Import `ExtractionResultSchema` from `@/types/ai`.
Import `openai` from `@/lib/openai`.
Import `getExtractionSystemPrompt` from `@/lib/prompts`.

The extraction prompt must instruct GPT to:
- Target 5-10 entries per session (per user decision)
- Use only categories from the predefined set (Preference, Treatment, Lifestyle, Health, Allergy, Style)
- Preserve original spoken language in source_quote field
- Write title field in the locale language
- Assign confidence_score between 0 and 1
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Verify the route exports POST and maxDuration. Verify zodResponseFormat is used (not JSON mode).</verify>
  <done>Extraction route accepts transcript + locale, calls GPT with structured output, returns schema-valid entries array with categories, titles, source quotes, and confidence scores.</done>
</task>

<task type="auto">
  <name>Task 2: GPT session summary API route</name>
  <files>src/app/api/ai/summarize/route.ts</files>
  <action>
Create `src/app/api/ai/summarize/route.ts`:
- Export `maxDuration = 60`
- Export async `POST` handler that:
  1. Parses JSON body: `{ transcript: string, locale: string }`
  2. Validates `transcript` exists and is non-empty — return 400 if missing
  3. Gets system prompt via `getSummarySystemPrompt(locale)` from `@/lib/prompts`
  4. Calls `openai.chat.completions.parse()` with:
     - `model: 'gpt-4o-mini'`
     - System message with the locale-aware prompt
     - User message: `"Summarize this salon session transcript:\n\n${transcript}"`
     - `response_format: zodResponseFormat(SummaryResultSchema, 'summary_result')`
  5. Extracts parsed result: `completion.choices[0].message.parsed`
  6. Returns `NextResponse.json(result)` — the `{ summary: string }` object
  7. Wraps in try/catch — on error, return 500 `{ error: 'Summary generation failed' }`

Import `SummaryResultSchema` from `@/types/ai`.
Import `zodResponseFormat` from `openai/helpers/zod`.
Import `openai` from `@/lib/openai`.
Import `getSummarySystemPrompt` from `@/lib/prompts`.

The summary should be 2-4 sentences of prose in the locale language.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Verify the route exports POST and maxDuration.</verify>
  <done>Summary route accepts transcript + locale, calls GPT with structured output, returns a prose summary string in the requested locale language.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `src/app/api/ai/extract/route.ts` exports POST, uses zodResponseFormat with ExtractionResultSchema
- `src/app/api/ai/summarize/route.ts` exports POST, uses zodResponseFormat with SummaryResultSchema
- Both routes import from `@/lib/openai`, `@/lib/prompts`, and `@/types/ai`
- Both routes use `gpt-4o-mini` model
- Both routes include locale-aware system prompts
</verification>

<success_criteria>
Both GPT API routes compile, use structured outputs for schema-guaranteed responses, and respect the locale parameter for output language. Ready to be called in parallel by the pipeline orchestration (Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-pipeline/02-02-SUMMARY.md`
</output>
