---
phase: 02-ai-pipeline
plan: "03"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/review/ReviewScreen.tsx
  - src/components/review/EntryCard.tsx
  - src/components/review/ReviewHeader.tsx
autonomous: true

must_haves:
  truths:
    - "Review screen displays AI summary at the top, editable inline"
    - "Entries render as a vertical card list with category tag, title, source quote, and confidence score badge"
    - "User can click any entry field to edit it inline — no modals"
    - "User can remove an entry from the list"
    - "User can add a new entry via + Add Entry button"
    - "Full transcript is visible side by side with the entries column"
    - "Confirm button submits the reviewed entries and summary"
  artifacts:
    - path: "src/components/review/ReviewScreen.tsx"
      provides: "Top-level review screen with two-column layout (transcript | entries), summary header, confirm button"
      min_lines: 80
    - path: "src/components/review/EntryCard.tsx"
      provides: "Single entry card with inline-editable fields, category color tag, confidence badge, remove button"
      min_lines: 40
    - path: "src/components/review/ReviewHeader.tsx"
      provides: "Editable AI summary section at top of review screen"
      min_lines: 20
  key_links:
    - from: "src/components/review/ReviewScreen.tsx"
      to: "src/types/ai.ts"
      via: "imports Entry type for form fields"
      pattern: "import.*Entry.*from.*types/ai"
    - from: "src/components/review/ReviewScreen.tsx"
      to: "react-hook-form"
      via: "useForm + useFieldArray for dynamic entry list"
      pattern: "useFieldArray"
---

<objective>
Build the human review screen where staff can edit, add, and remove AI-extracted entries before saving.

Purpose: AI-06 requires human review before save. This screen displays the AI output (summary + entries + transcript) in an editable format, letting staff correct mistakes, remove irrelevant entries, and add missed ones. Uses React Hook Form's useFieldArray for the dynamic entry list.
Output: Complete review screen components, ready to receive data from the pipeline (Plan 04).
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ai-pipeline/02-RESEARCH.md
@.planning/phases/02-ai-pipeline/02-CONTEXT.md
@.planning/phases/02-ai-pipeline/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: EntryCard and ReviewHeader components</name>
  <files>src/components/review/EntryCard.tsx, src/components/review/ReviewHeader.tsx</files>
  <action>
Create `src/components/review/EntryCard.tsx`:
- A card component representing one AI-extracted entry in the review list
- Props: `index: number`, `control: Control` (from react-hook-form), `onRemove: () => void`
- Uses `useController` or `register` from the parent form's `control` for inline editing
- Displays:
  - Category tag with color coding — use a color map: Preference=blue, Treatment=green, Lifestyle=purple, Health=red, Allergy=orange, Style=pink. Render as a small badge/pill.
  - Title field — rendered as text that becomes an input on click (inline edit). Use `contentEditable` or a controlled input that looks like text until focused.
  - Source quote — rendered in italics, smaller text. Also inline-editable.
  - Confidence score — displayed as a percentage badge (e.g., "95%"). Multiply the 0-1 value by 100 and round. Read-only display (not editable by user).
  - Category selector — a dropdown/select to change the category (uses the ENTRY_CATEGORIES constant)
- Remove button (X or trash icon) that calls `onRemove`
- Style with Tailwind: dark theme card with border, rounded corners, padding. Hover state on remove button.

Create `src/components/review/ReviewHeader.tsx`:
- Displays the AI-generated summary at the top of the review screen
- Props: receives `register` or `control` from parent form for the `summary` field
- Summary text is displayed in a textarea or contentEditable block — editable inline per user decision
- Label: "AI Summary" or similar
- Style: slightly larger text area, dark themed, subtle border
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Verify both components import from react-hook-form and types/ai.</verify>
  <done>EntryCard renders an entry with all four fields (category tag, title, source quote, confidence badge), supports inline editing, and has a remove button. ReviewHeader renders an editable AI summary textarea.</done>
</task>

<task type="auto">
  <name>Task 2: ReviewScreen with two-column layout and useFieldArray</name>
  <files>src/components/review/ReviewScreen.tsx</files>
  <action>
Create `src/components/review/ReviewScreen.tsx`:
- Top-level component for the human review screen (AI-06)
- Props interface: `{ transcript: string, entries: Entry[], summary: string, onConfirm: (data: { entries: Entry[], summary: string }) => void }`
- Uses `useForm` with `zodResolver` for validation:
  - Form schema: `z.object({ summary: z.string().min(1), entries: z.array(EntrySchema) })`
  - Default values: `{ summary, entries }` from props
- Uses `useFieldArray({ control, name: 'entries' })` for dynamic entry list
- Layout (per user decision — transcript side by side with entries):
  - Two-column grid on desktop (lg:grid-cols-2), single column on mobile
  - Left column: Full transcript text in a scrollable panel with "Transcript" heading. Read-only. Styled as a pre/prose block so staff can cross-reference source quotes.
  - Right column: ReviewHeader (editable summary) at top, then vertical stack of EntryCard components, then + Add Entry button at bottom
- + Add Entry button: calls `append({ category: 'Other', title: '', source_quote: '', confidence_score: 1 })` to add a blank entry
- Confirm button at the bottom: submits the form via `handleSubmit(onConfirm)`
- Style with Tailwind dark theme: dark background panels, scrollable transcript area (max-h with overflow-y-auto), gap between columns

Import EntryCard and ReviewHeader from the same directory.
Import Entry, EntrySchema, ENTRY_CATEGORIES from `@/types/ai`.
Import useForm, useFieldArray from react-hook-form.
Import zodResolver from @hookform/resolvers/zod.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Verify useFieldArray is used (not manual useState array). Verify two-column layout with transcript on left, entries on right. Verify + Add Entry button exists. Verify Confirm button exists.</verify>
  <done>ReviewScreen renders a two-column layout with transcript panel and editable entry cards, supports add/edit/remove entries, shows editable AI summary, and calls onConfirm with the final reviewed data.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- ReviewScreen accepts transcript, entries, summary props and renders two-column layout
- EntryCard shows category color tag, inline-editable title, source quote, confidence badge, remove button
- ReviewHeader shows editable summary textarea
- useFieldArray manages the entries array (not useState)
- + Add Entry button appends a new blank entry
- Confirm button submits the form
- All components use Tailwind dark theme styling
</verification>

<success_criteria>
Complete review screen UI that displays AI output and allows human editing before save. Components are self-contained and accept data via props — ready to receive pipeline output in Plan 04.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-pipeline/02-03-SUMMARY.md`
</output>
