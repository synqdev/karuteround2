---
phase: 05-staff-profiles
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/karute.ts
  - src/components/karute/KaruteHeader.tsx
  - src/components/customers/KaruteHistoryList.tsx
  - src/app/[locale]/(app)/customers/[id]/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Karute detail view displays the staff member's name who created the record"
    - "Customer profile history list displays the staff member's name on each karute row"
  artifacts:
    - path: "src/lib/supabase/karute.ts"
      provides: "Staff name via profiles join on getKaruteRecord query"
      contains: "profiles:staff_profile_id"
    - path: "src/components/karute/KaruteHeader.tsx"
      provides: "Staff name rendered in karute detail header"
      contains: "staffName"
    - path: "src/components/customers/KaruteHistoryList.tsx"
      provides: "Actual staff name per history row instead of static placeholder"
      contains: "staff_name"
    - path: "src/app/[locale]/(app)/customers/[id]/page.tsx"
      provides: "Profiles join in customer page karute query"
      contains: "profiles:staff_profile_id"
  key_links:
    - from: "src/lib/supabase/karute.ts"
      to: "profiles table"
      via: "PostgREST join profiles:staff_profile_id ( id, full_name )"
      pattern: "profiles:staff_profile_id.*full_name"
    - from: "src/components/karute/KaruteHeader.tsx"
      to: "karute.profiles"
      via: "reads staff name from joined profiles data"
      pattern: "profiles.*full_name"
    - from: "src/components/customers/KaruteHistoryList.tsx"
      to: "staff_name prop"
      via: "renders actual staff name from query data"
      pattern: "record\\.staff_name|record\\.profiles"
---

<objective>
Close the gap on Truth #3: "Every saved karute record displays the name of the staff member who created it."

The save path works (staff_profile_id written from cookie). The display path is broken — queries do not join the profiles table, and both display components render no staff name. This plan adds the profiles join to both queries and renders the staff name in KaruteHeader and KaruteHistoryList.

Purpose: Complete STAFF-03 requirement — staff attribution visible on karute records.
Output: Staff name displayed on karute detail view header and customer profile history list rows.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/supabase/karute.ts
@src/components/karute/KaruteHeader.tsx
@src/components/customers/KaruteHistoryList.tsx
@src/app/[locale]/(app)/customers/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add profiles join to karute queries and render staff name in both display components</name>
  <files>
    src/lib/supabase/karute.ts
    src/components/karute/KaruteHeader.tsx
    src/components/customers/KaruteHistoryList.tsx
    src/app/[locale]/(app)/customers/[id]/page.tsx
  </files>
  <action>
    **1. src/lib/supabase/karute.ts — Add profiles join to both the type-inference query and the runtime query:**

    In `_karuteWithRelationsQuery`, add `profiles:staff_profile_id ( id, full_name )` to the select string, after the `staff_profile_id` line and before the `customers:client_id` line. Do the same in `getKaruteRecord()`. Both select strings must match exactly so the inferred type includes the joined profiles data.

    The select string should include:
    ```
    staff_profile_id,
    profiles:staff_profile_id ( id, full_name ),
    customers:client_id ( id, name ),
    ```

    **2. src/components/karute/KaruteHeader.tsx — Render staff name:**

    Extract the staff name from the joined profiles data:
    ```ts
    const staffName =
      (karute as { profiles?: { full_name: string } | null }).profiles?.full_name ?? '—'
    ```
    This follows the same cast pattern already used for `customers` (line 17-18).

    Add a new div block after the date div (before the closing `</div>` of the flex container) to display the staff name:
    ```tsx
    <div>
      <p className="text-xs uppercase tracking-wide text-muted-foreground">
        {t('staff')}
      </p>
      <p className="text-sm text-foreground">{staffName}</p>
    </div>
    ```

    **3. src/app/[locale]/(app)/customers/[id]/page.tsx — Add profiles join to customer page karute query:**

    Change the karute_records select from:
    ```
    'id, created_at, summary, staff_profile_id, session_date'
    ```
    to:
    ```
    'id, created_at, summary, staff_profile_id, session_date, profiles:staff_profile_id ( full_name )'
    ```

    **4. src/components/customers/KaruteHistoryList.tsx — Replace placeholder with actual staff name:**

    Update the `KaruteRecord` interface to include the joined profile:
    ```ts
    interface KaruteRecord {
      id: string
      created_at: string
      summary: string | null
      session_date?: string
      staff_profile_id?: string | null
      profiles?: { full_name: string } | null
    }
    ```

    Replace lines 80-81 (the placeholder comment and static label) with:
    ```tsx
    <span className="text-sm text-muted-foreground shrink-0 w-20">
      {record.profiles?.full_name ?? t('profile.staff')}
    </span>
    ```
    Remove the placeholder comment `/* Staff name placeholder — Phase 5 will add actual staff attribution */`.

    **Important:** Do NOT add a `karute.staff` i18n key — the `karute.staff` translation likely does not exist. Check existing i18n files. If `karute.staff` is missing, use `t('staff')` which maps to the label used in KaruteHeader. The fallback in KaruteHistoryList uses `t('profile.staff')` which already exists in the customers namespace.
  </action>
  <verify>
    1. `npx tsc --noEmit` — zero TypeScript errors (the KaruteWithRelations type must include profiles from the join)
    2. Grep for the placeholder comment — must be gone: `grep -r "Phase 5 will add" src/` returns nothing
    3. Grep for `profiles:staff_profile_id` in `src/lib/supabase/karute.ts` — must appear in both the type query and getKaruteRecord
    4. Grep for `profiles:staff_profile_id` in `src/app/[locale]/(app)/customers/[id]/page.tsx` — must appear
    5. Grep for `staffName` in `src/components/karute/KaruteHeader.tsx` — must appear
    6. Grep for `record.profiles?.full_name` in `src/components/customers/KaruteHistoryList.tsx` — must appear
  </verify>
  <done>
    Staff member's full_name is joined via profiles:staff_profile_id in both karute queries. KaruteHeader renders the staff name alongside customer and date. KaruteHistoryList renders the actual staff name per row, falling back to the static label only when no profile is joined. The Phase 5 placeholder comment is removed. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. No placeholder comments remain: `grep -r "Phase 5 will add" src/` returns empty
3. Both queries include `profiles:staff_profile_id ( ... full_name )` join
4. KaruteHeader JSX has a staff name section
5. KaruteHistoryList renders `record.profiles?.full_name` per row
</verification>

<success_criteria>
Truth #3 from Phase 5 verification is satisfied: every saved karute record displays the name of the staff member who created it — both on the karute detail view (KaruteHeader) and the customer profile history list (KaruteHistoryList).
</success_criteria>

<output>
After completion, create `.planning/phases/05-staff-profiles/05-04-SUMMARY.md`
</output>
