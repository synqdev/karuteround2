---
phase: 05-staff-profiles
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/(app)/settings/page.tsx
  - src/components/staff/StaffList.tsx
  - src/components/staff/StaffForm.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can see all staff profiles listed in the Settings page"
    - "Admin can create a new staff profile with name and display info"
    - "Admin can edit an existing staff profile's name and display info"
    - "Admin can delete a staff profile"
  artifacts:
    - path: "src/app/(app)/settings/page.tsx"
      provides: "Settings page with staff management section"
      contains: "StaffList"
    - path: "src/components/staff/StaffList.tsx"
      provides: "Staff list with edit and delete actions"
      contains: "deleteStaff"
    - path: "src/components/staff/StaffForm.tsx"
      provides: "Create/edit staff form with react-hook-form + Zod"
      contains: "staffProfileSchema"
  key_links:
    - from: "src/app/(app)/settings/page.tsx"
      to: "src/lib/staff.ts"
      via: "getStaffList() call in Server Component"
      pattern: "getStaffList"
    - from: "src/components/staff/StaffForm.tsx"
      to: "src/actions/staff.ts"
      via: "calls createStaff or updateStaff Server Action"
      pattern: "(createStaff|updateStaff)"
    - from: "src/components/staff/StaffList.tsx"
      to: "src/actions/staff.ts"
      via: "calls deleteStaff Server Action"
      pattern: "deleteStaff"
---

<objective>
Build the staff management UI in the Settings page: a list of staff profiles with create, edit, and delete functionality.

Purpose: Fulfills STAFF-01 — staff profiles with name and display info, manageable by admin through the Settings page.
Output: Settings page, StaffList component, StaffForm component (create/edit).
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-staff-profiles/05-RESEARCH.md
@.planning/phases/05-staff-profiles/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings page and StaffList component</name>
  <files>
    src/app/(app)/settings/page.tsx
    src/components/staff/StaffList.tsx
  </files>
  <action>
1. **Create `src/app/(app)/settings/page.tsx`** (Server Component):
   - Import `getStaffList` from `@/lib/staff`
   - Fetch staff list server-side
   - Render page with heading "Settings" and a "Staff" section
   - Pass `staffList` to `StaffList` client component
   - Include a "Add Staff" button that opens the StaffForm in create mode
   - Use dark theme styling consistent with the app (Tailwind classes)
   - **Important:** This is nested inside `app/(app)/` to inherit the authenticated layout's middleware check — do NOT create a top-level `/settings` route

2. **Create `src/components/staff/StaffList.tsx`** (`'use client'`):
   - Receives `staffList` as prop (array of `{ id, name, display_info, created_at }`)
   - Renders a list/table of staff members showing name and display_info
   - Each row has Edit and Delete action buttons
   - Delete button calls `deleteStaff(id)` Server Action with a confirmation dialog (browser `confirm()` is fine for v1)
   - Edit button opens StaffForm in edit mode (use local state to track which staff is being edited)
   - Empty state: show a message like "No staff members yet. Add one to get started."
   - Use shadcn/ui `Table` if available, otherwise a styled Tailwind table
   - Use shadcn/ui `Button` for actions
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Settings page renders at `/settings` path inside the `(app)` route group
    - StaffList component renders staff members and has edit/delete action buttons
  </verify>
  <done>
    - Settings page exists at `src/app/(app)/settings/page.tsx` and fetches staff list server-side
    - StaffList component displays staff names and display info
    - Delete action calls deleteStaff Server Action
    - Edit action transitions to edit mode
    - Empty state handled
  </done>
</task>

<task type="auto">
  <name>Task 2: StaffForm component for create and edit</name>
  <files>
    src/components/staff/StaffForm.tsx
  </files>
  <action>
1. **Create `src/components/staff/StaffForm.tsx`** (`'use client'`):
   - Dual-mode form: create (no initial values) and edit (pre-filled with existing staff data)
   - Props: `mode: 'create' | 'edit'`, `staff?: { id: string; name: string; display_info?: string }`, `onClose: () => void`
   - Use `react-hook-form` with `zodResolver` and `staffProfileSchema` from `@/lib/validations/staff.ts`
   - Fields:
     - Name: text input, required (validated by Zod — min 1, max 100)
     - Display Info: textarea, optional (max 500 chars)
   - On submit:
     - Create mode: call `createStaff(data)` Server Action
     - Edit mode: call `updateStaff(staff.id, data)` Server Action
   - Show loading state during submission (disable submit button, show spinner or "Saving...")
   - On success: call `onClose()` to dismiss the form
   - On error: display error message (from thrown error) using a toast or inline error display
   - Render as a dialog/modal (shadcn/ui `Dialog` if available) or an inline form section
   - Use shadcn/ui `Input`, `Textarea`, `Button` components
   - Dark theme styling consistent with the app

**Integration with StaffList:** The Settings page should manage state for showing/hiding StaffForm. When "Add Staff" is clicked, show StaffForm in create mode. When "Edit" is clicked on a row, show StaffForm in edit mode with that staff's data. When StaffForm calls onClose, hide the form and the list auto-refreshes (Server Actions call revalidatePath).
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - StaffForm renders name input and display_info textarea
    - Form validates inputs against Zod schema before submission
    - Create mode calls createStaff, edit mode calls updateStaff
  </verify>
  <done>
    - StaffForm component exists with create and edit modes
    - Form uses react-hook-form + zodResolver for validation
    - Name field is required (min 1, max 100); display_info is optional (max 500)
    - Submit calls appropriate Server Action and closes on success
    - Error state displays to user
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Settings page is accessible at `/settings` within the protected `(app)` route group
- Staff can be listed, created, edited, and deleted through the UI
</verification>

<success_criteria>
- Settings page renders staff list fetched server-side
- Create form adds a new staff profile visible in the list after save
- Edit form updates an existing staff profile
- Delete removes a staff profile from the list
- All form validation enforced (name required, length limits)
</success_criteria>

<output>
After completion, create `.planning/phases/05-staff-profiles/05-02-SUMMARY.md`
</output>
