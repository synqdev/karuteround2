---
phase: 05-staff-profiles
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/(app)/settings/page.tsx
  - src/components/staff/StaffList.tsx
  - src/components/staff/StaffForm.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can see all staff profiles listed in the Settings page"
    - "Admin can create a new staff profile with name"
    - "Admin can edit an existing staff profile's name"
    - "Admin can delete a staff profile with appropriate guards"
    - "Active staff badge is shown next to the currently active staff member"
    - "Each staff row shows 'Added [date]' metadata"
    - "Toast notifications show for success and error feedback"
  artifacts:
    - path: "src/app/(app)/settings/page.tsx"
      provides: "Settings page with staff management section"
      contains: "StaffList"
    - path: "src/components/staff/StaffList.tsx"
      provides: "Staff list with edit and delete actions, active badge, added date"
      contains: "deleteStaff"
    - path: "src/components/staff/StaffForm.tsx"
      provides: "Create/edit staff form with react-hook-form + Zod"
      contains: "staffProfileSchema"
  key_links:
    - from: "src/app/(app)/settings/page.tsx"
      to: "src/lib/staff.ts"
      via: "getStaffList() and getActiveStaffId() calls in Server Component"
      pattern: "(getStaffList|getActiveStaffId)"
    - from: "src/components/staff/StaffForm.tsx"
      to: "src/actions/staff.ts"
      via: "calls createStaff or updateStaff Server Action"
      pattern: "(createStaff|updateStaff)"
    - from: "src/components/staff/StaffList.tsx"
      to: "src/actions/staff.ts"
      via: "calls deleteStaff Server Action"
      pattern: "deleteStaff"
---

<objective>
Build the staff management UI in the Settings page: a list of staff profiles with create, edit, and delete functionality, active badge, and added-date metadata.

Purpose: Fulfills STAFF-01 — staff profiles with name, manageable by admin through the Settings page.
Output: Settings page, StaffList component, StaffForm component (create/edit).
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-staff-profiles/05-RESEARCH.md
@.planning/phases/05-staff-profiles/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings page and StaffList component</name>
  <files>
    src/app/(app)/settings/page.tsx
    src/components/staff/StaffList.tsx
  </files>
  <action>
1. **Create `src/app/(app)/settings/page.tsx`** (Server Component):
   - Import `getStaffList` and `getActiveStaffId` from `@/lib/staff`
   - Fetch staff list and active staff ID server-side
   - Render page with heading "Settings" at top, then "Staff Members" section heading
   - **Staff section only — no placeholder sections** (per locked decision)
   - "Add Staff" button positioned top-right of the "Staff Members" section heading
   - Pass `staffList` and `activeStaffId` to `StaffList` client component
   - Use dark theme styling consistent with the app (Tailwind classes)
   - **Important:** This is nested inside `app/(app)/` to inherit the authenticated layout's middleware check — do NOT create a top-level `/settings` route

2. **Create `src/components/staff/StaffList.tsx`** (`'use client'`):
   - Receives props: `staffList: { id: string; name: string; created_at: string }[]`, `activeStaffId: string | null`
   - Renders a simple vertical list of staff members showing:
     - **Name** as primary text
     - **"Added [date]"** metadata below or beside the name — format `created_at` as a readable date (e.g., "Added Mar 13, 2026")
     - **"Active" badge** next to the staff member whose id matches `activeStaffId` (per locked decision)
     - **Edit icon/button** — opens StaffForm in edit mode
     - **Delete button** — uses red/destructive variant (per locked decision)
   - Delete button behavior:
     - Calls `deleteStaff(id)` Server Action
     - The server action handles guards (karute records check, last staff check) and returns appropriate errors
     - On error from deleteStaff, show a **toast notification** with the error message
     - For staff with no records, show confirmation dialog: "Delete [Name]? This action cannot be undone." (browser `confirm()` is fine for v1)
   - Edit button opens StaffForm in edit mode (use local state to track which staff is being edited)
   - **Empty state:** "No staff members yet" text with a prominent "Add Staff Member" button (per locked decision)
   - **Alphabetical sort** — list is already sorted from server, no manual reordering
   - **No pagination, no search** — show all staff (per locked decision)
   - Use shadcn/ui `Button` for actions
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Settings page renders at `/settings` path inside the `(app)` route group
    - StaffList component renders staff names with "Added [date]" metadata and edit/delete action buttons
    - Active badge is shown next to the staff member matching activeStaffId
    - Empty state shows "No staff members yet" with add button
  </verify>
  <done>
    - Settings page exists at `src/app/(app)/settings/page.tsx` and fetches staff list + active staff ID server-side
    - StaffList displays staff names with "Added [date]" metadata
    - "Active" badge shown next to currently active staff member
    - Delete action calls deleteStaff Server Action; errors shown as toast
    - Delete button uses red/destructive variant
    - Edit action transitions to edit mode
    - Empty state: "No staff members yet" + prominent add button
  </done>
</task>

<task type="auto">
  <name>Task 2: StaffForm component for create and edit</name>
  <files>
    src/components/staff/StaffForm.tsx
  </files>
  <action>
1. **Create `src/components/staff/StaffForm.tsx`** (`'use client'`):
   - Dual-mode form: create (no initial values) and edit (pre-filled with existing staff data)
   - Props: `mode: 'create' | 'edit'`, `staff?: { id: string; name: string }`, `onClose: () => void`
   - Modal title changes based on mode: "Add Staff Member" (create) / "Edit Staff Member" (edit) — per locked decision
   - Use `react-hook-form` with `zodResolver` and `staffProfileSchema` from `@/lib/validations/staff.ts`
   - Fields:
     - **Name only:** single text input, required (validated by Zod — min 1, max 100)
     - **No display_info field, no role, no avatar, no initials** (per locked decision: "Name only — single text field")
   - Name required validation only — no duplicate check (per locked decision)
   - On submit:
     - Create mode: call `createStaff(data)` Server Action
     - Edit mode: call `updateStaff(staff.id, data)` Server Action
   - Show loading spinner on save button during submission (per locked decision: "Modal has loading spinner on save button")
   - On success: show success toast notification, call `onClose()` to dismiss the modal
   - On error: show error toast notification (per locked decision: "Toast notification for success/error feedback (not inline in modal)")
   - **Toast-only feedback — no inline error display in modal** (per locked decision)
   - Render as a dialog/modal using shadcn/ui `Dialog`
   - Use shadcn/ui `Input`, `Button` components
   - Dark theme styling consistent with the app

**Integration with StaffList:** The Settings page should manage state for showing/hiding StaffForm. When "Add Staff" is clicked, show StaffForm in create mode. When "Edit" is clicked on a row, show StaffForm in edit mode with that staff's data. When StaffForm calls onClose, hide the form and the list auto-refreshes (Server Actions call revalidatePath).
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - StaffForm renders name input only (no display_info textarea)
    - Form validates inputs against Zod schema before submission
    - Create mode calls createStaff, edit mode calls updateStaff
    - Modal title changes between "Add Staff Member" and "Edit Staff Member"
    - Success/error feedback uses toast notifications only (not inline)
    - Save button shows loading spinner during submission
  </verify>
  <done>
    - StaffForm component exists with create and edit modes
    - Form uses react-hook-form + zodResolver for validation
    - Name field only — required (min 1, max 100), no display_info
    - Modal title: "Add Staff Member" / "Edit Staff Member"
    - Loading spinner on save button during submission
    - Toast-only feedback for success and error (no inline errors in modal)
    - Submit calls appropriate Server Action and closes on success
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Settings page is accessible at `/settings` within the protected `(app)` route group
- Staff can be listed, created, edited, and deleted through the UI
- Active badge shown next to active staff member
- "Added [date]" metadata shown on each staff row
- Toast notifications used for all success/error feedback
</verification>

<success_criteria>
- Settings page renders staff list fetched server-side with active staff ID
- "Active" badge displayed next to active staff member
- Each staff row shows "Added [date]" formatted metadata
- Create form adds a new staff profile (name only) visible in the list after save
- Edit form updates an existing staff profile's name
- Delete shows confirmation for eligible staff, blocks with toast for staff with records or last staff
- Delete button uses red/destructive variant
- All form validation enforced (name required, max 100)
- All feedback via toast notifications (not inline in modal)
</success_criteria>

<output>
After completion, create `.planning/phases/05-staff-profiles/05-02-SUMMARY.md`
</output>
