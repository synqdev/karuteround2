---
phase: 05-staff-profiles
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/staff/StaffSwitcher.tsx
  - src/app/(app)/layout.tsx
  - src/actions/karute.ts
autonomous: true

must_haves:
  truths:
    - "User can switch the active staff member from a header dropdown without logging out"
    - "Active staff selection persists across page refresh (cookie-backed)"
    - "Header displays the currently active staff member's name"
    - "When no cookie exists, the first alphabetical staff member is auto-selected"
    - "Karute save flow reads staff_id from active-staff cookie, not from client-supplied parameter"
  artifacts:
    - path: "src/components/staff/StaffSwitcher.tsx"
      provides: "Header dropdown for switching active staff"
      contains: "setActiveStaff"
    - path: "src/app/(app)/layout.tsx"
      provides: "Layout integrating StaffSwitcher with server-fetched data"
      contains: "StaffSwitcher"
    - path: "src/actions/karute.ts"
      provides: "Karute save action using getActiveStaffId() for attribution"
      contains: "getActiveStaffId"
  key_links:
    - from: "src/app/(app)/layout.tsx"
      to: "src/lib/staff.ts"
      via: "getStaffList + getActiveStaffId for server-side data"
      pattern: "(getStaffList|getActiveStaffId)"
    - from: "src/components/staff/StaffSwitcher.tsx"
      to: "src/actions/staff.ts"
      via: "calls setActiveStaff Server Action on dropdown selection"
      pattern: "setActiveStaff"
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/staff/StaffSwitcher.tsx"
      via: "passes staffList and activeStaff as props"
      pattern: "StaffSwitcher.*staffList"
    - from: "src/actions/karute.ts"
      to: "src/lib/staff.ts"
      via: "reads active staff ID from cookie for attribution"
      pattern: "getActiveStaffId"
---

<objective>
Build the header staff switcher dropdown, integrate it into the app layout, and wire karute attribution to read staff_id from the active-staff cookie.

Purpose: Fulfills STAFF-02 (staff switcher in header) and STAFF-03 (staff attribution — karute saves use the active staff cookie, not client-supplied data).
Output: StaffSwitcher client component, updated app layout, and updated karute save action.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-staff-profiles/05-RESEARCH.md
@.planning/phases/05-staff-profiles/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: StaffSwitcher component and layout integration</name>
  <files>
    src/components/staff/StaffSwitcher.tsx
    src/app/(app)/layout.tsx
  </files>
  <action>
1. **Create `src/components/staff/StaffSwitcher.tsx`** (`'use client'`):
   - Props: `staffList: { id: string; name: string }[]`, `activeStaff: { id: string; name: string } | null`
   - Renders a simple dropdown (shadcn/ui `DropdownMenu`) positioned on the right side of the header (per locked decision)
   - Trigger: Button showing the active staff name only — no role, no avatar, no initials (per locked decision: "Switcher displays name only")
   - Content: List of all staff members from `staffList`, including the current active one (per locked decision: "Simple dropdown with all staff including current")
   - Each item calls `setActiveStaff(staff.id)` Server Action on click — immediate switch, no confirmation (per locked decision)
   - Visual indicator (checkmark or bold) on the currently active staff member
   - If `staffList` is empty, show a disabled button with text "No Staff"
   - Dark theme styling

2. **Update `src/app/(app)/layout.tsx`** (Server Component):
   - **Read the existing layout first** — do not overwrite existing content. Add staff switcher to the header area.
   - Import `getStaffList` from `@/lib/staff` and `getActiveStaffId` from `@/lib/staff`
   - Fetch staff list and active staff ID server-side
   - Resolve the active staff object: find matching staff from list by cookie ID. If no cookie or cookie ID not found in list, fall back to first staff member alphabetically (`staffList[0]` since list is ordered by name).
   - **Auto-select on first load:** If no `active_staff_id` cookie exists but staff members exist, call `setActiveStaff(staffList[0].id)` to set the cookie for future navigations (per locked decision: "Auto-select first alphabetical on first login").
   - Pass `staffList` and resolved `activeStaff` as props to `<StaffSwitcher />`
   - Position the switcher on the right side of the header (per locked decision)

**Key architectural points:**
- The layout fetches data server-side and passes to the client component — the client component never fetches directly
- `setActiveStaff` writes a cookie + calls `revalidatePath('/', 'layout')`, which re-renders the layout and updates the displayed name
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - StaffSwitcher component renders with dropdown menu showing name only
    - Layout imports and renders StaffSwitcher with server-fetched data
    - Active staff resolved from cookie with fallback to first staff member
  </verify>
  <done>
    - StaffSwitcher component renders active staff name only in header with dropdown to switch
    - Clicking a different staff member calls setActiveStaff (immediate, no confirmation)
    - Layout fetches staff list and active staff ID server-side, passes as props
    - Auto-selection sets cookie for first staff member when no cookie exists
    - Staff selection persists across page refresh via cookie
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire karute attribution to active-staff cookie</name>
  <files>
    src/actions/karute.ts
  </files>
  <action>
**This task completes STAFF-03 attribution wiring.** Phase 4 deferred staff attribution to Phase 5 via TODO. This task fulfills that deferral.

1. **Read `src/actions/karute.ts`** to understand the current save flow.

2. **Modify the karute save action** (likely `saveKarute` or similar):
   - Import `getActiveStaffId` from `@/lib/staff`
   - **Remove** the client-supplied `staffId` parameter from the save action's input type/schema. Staff ID must NOT come from client form fields.
   - At the top of the save action, call `const staffId = await getActiveStaffId()`
   - If `staffId` is null, throw an error: "No active staff member selected. Please select a staff member from the header."
   - Use this server-read `staffId` when inserting into `karute_records.staff_id`
   - Remove any TODO comments about wiring staff_id in Phase 5

3. **Also check `src/lib/karute/draft.ts`** (or wherever draft types are defined):
   - If `KaruteDraft` type includes `staffId`, remove it — staff ID is no longer part of the draft since it comes from the cookie at save time, not from the client
   - Update any draft save/load logic accordingly

**Why this matters:** Without this wiring, karute records would either have no staff attribution or rely on client-supplied data (which is insecure and unreliable). The active-staff cookie is the single source of truth.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `src/actions/karute.ts` imports and calls `getActiveStaffId()` from `@/lib/staff`
    - No client-supplied `staffId` parameter in the save action input
    - Save action throws if no active staff is selected
    - No remaining TODO comments about Phase 5 staff wiring
  </verify>
  <done>
    - Karute save action reads staff_id from active-staff cookie via getActiveStaffId()
    - Client-supplied staffId parameter removed from save action input
    - Save action throws clear error if no active staff selected
    - TODO comments about Phase 5 staff wiring removed
    - KaruteDraft type updated to remove staffId if present
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Header displays active staff member name only
- Switching staff updates the header display immediately
- Page refresh retains the selected staff member
- Karute save flow uses cookie-based staff_id, not client-supplied
</verification>

<success_criteria>
- Staff switcher dropdown is visible in the app header, right side
- Selecting a staff member updates the cookie and re-renders the header with the new name
- Active staff persists across browser refresh (cookie lasts 30 days)
- First-time visitors with no cookie get the first staff member auto-selected
- Empty staff list shows disabled "No Staff" button
- Karute save action reads staff_id from cookie via getActiveStaffId(), not from client
- Save action throws if no active staff selected
</success_criteria>

<output>
After completion, create `.planning/phases/05-staff-profiles/05-03-SUMMARY.md`
</output>
