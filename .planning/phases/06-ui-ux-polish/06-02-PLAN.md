---
phase: 06-ui-ux-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/sidebar.tsx
  - src/components/layout/top-bar.tsx
  - src/components/layout/locale-toggle.tsx
  - src/components/layout/theme-toggle.tsx
autonomous: true

must_haves:
  truths:
    - "Sidebar is 90px wide, icon-only with labels beneath icons, bg-[#4a4a4a], fixed height"
    - "Active sidebar item is detected via pathname.startsWith(href) and highlighted with text-white vs text-white/60"
    - "Sidebar links to Recording (/sessions), Customers (/customers), Karute (/karute), and Settings (/settings)"
    - "TopBar contains locale toggle, theme toggle, and staff switcher placeholder"
    - "Locale toggle switches between EN and JP using router.replace from @/i18n/navigation"
    - "Theme toggle switches between light and dark using next-themes useTheme"
  artifacts:
    - path: "src/components/layout/sidebar.tsx"
      provides: "Custom 90px icon-only sidebar with active state detection"
      contains: "pathname.startsWith"
    - path: "src/components/layout/top-bar.tsx"
      provides: "Header bar with locale toggle, theme toggle, staff switcher slot"
      contains: "TopBar"
    - path: "src/components/layout/locale-toggle.tsx"
      provides: "EN/JP locale switch button using next-intl typed navigation"
      contains: "useRouter"
    - path: "src/components/layout/theme-toggle.tsx"
      provides: "Light/dark theme switch using next-themes"
      contains: "useTheme"
  key_links:
    - from: "src/components/layout/sidebar.tsx"
      to: "@/i18n/navigation"
      via: "usePathname for active route detection, Link for navigation"
      pattern: "usePathname.*@/i18n/navigation"
    - from: "src/components/layout/locale-toggle.tsx"
      to: "@/i18n/navigation"
      via: "router.replace(pathname, { locale: next })"
      pattern: "router\\.replace"
    - from: "src/components/layout/theme-toggle.tsx"
      to: "next-themes"
      via: "useTheme hook setTheme"
      pattern: "useTheme"
---

<objective>
Build the custom sidebar navigation and TopBar with locale/theme toggles matching the reference app.

Purpose: The sidebar is the primary navigation pattern. The TopBar locale toggle is the key bilingual UX. Both are layout-level components that every page depends on.
Output: Custom 90px sidebar with active state, TopBar with working EN/JP toggle and light/dark toggle.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ui-ux-polish/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Custom sidebar with icon navigation and active state</name>
  <files>
    src/components/layout/sidebar.tsx
  </files>
  <action>
    Create `src/components/layout/sidebar.tsx`:
    - 'use client' directive
    - Import usePathname from '@/i18n/navigation' (NOT from next/navigation -- critical)
    - Import Link from '@/i18n/navigation' (NOT from next/link)
    - Import useTranslations from 'next-intl'

    Define NAV_ROUTES array:
    ```
    [
      { id: 'recording', href: '/sessions', labelKey: 'recording', icon: MicIcon },
      { id: 'customers', href: '/customers', labelKey: 'customers', icon: UsersIcon },
      { id: 'karute', href: '/karute', labelKey: 'karute', icon: ClipboardIcon },
      { id: 'settings', href: '/settings', labelKey: 'settings', icon: SettingsIcon },
    ]
    ```

    Active detection: `const activeId = NAV_ROUTES.find((r) => pathname.startsWith(r.href))?.id`

    Sidebar structure:
    - Fixed width: w-[90px]
    - Background: bg-[#4a4a4a]
    - Rounded corners: rounded-[28px]
    - Full height within the flex container (h-full)
    - Vertical flex column with items centered
    - Each nav button: flex flex-col items-center gap-1 px-2 py-3, min-h-[44px] min-w-[44px] for touch targets
    - Active state: text-white when active, text-white/60 hover:text-white/90 when inactive
    - Icons: Create simple SVG icon components (Mic, Users, Clipboard, Settings) inline or as separate small components at the bottom of the file
    - Labels: text-[10px] font-medium under each icon, using t('sidebar.{labelKey}')

    Do NOT use the shadcn Sidebar primitive -- this is a custom sidebar matching the reference app (per research recommendation).
    Do NOT use emoji icons -- use proper SVG icons.
  </action>
  <verify>
    Run: `npm run type-check` -- exits 0.
    Check: `grep "usePathname.*@/i18n/navigation" src/components/layout/sidebar.tsx` -- found (correct import source).
    Check: `grep "pathname.startsWith" src/components/layout/sidebar.tsx` -- found (active detection).
    Check: `grep "bg-\\[#4a4a4a\\]" src/components/layout/sidebar.tsx` -- found.
  </verify>
  <done>
    Custom 90px sidebar component with 4 nav items (Recording, Customers, Karute, Settings), active state via pathname.startsWith, bg-[#4a4a4a], touch-friendly targets. Imports from @/i18n/navigation.
  </done>
</task>

<task type="auto">
  <name>Task 2: TopBar with locale toggle and theme toggle</name>
  <files>
    src/components/layout/top-bar.tsx
    src/components/layout/locale-toggle.tsx
    src/components/layout/theme-toggle.tsx
  </files>
  <action>
    Create `src/components/layout/locale-toggle.tsx`:
    - 'use client' directive
    - Import useLocale from 'next-intl'
    - Import useRouter, usePathname from '@/i18n/navigation' (NOT next/navigation)
    - toggleLocale function: `const next = locale === 'ja' ? 'en' : 'ja'; router.replace(pathname as Parameters<typeof router.replace>[0], { locale: next })`
    - Render a button showing locale state: "EN" when locale is 'en', "JP" when locale is 'ja'
    - Button styling: text-sm font-medium, border rounded-md px-3 py-1.5, text-white/80 hover:text-white, min-h-[44px] for touch target
    - The cast `as Parameters<typeof router.replace>[0]` is needed for type safety with the typed navigation layer

    Create `src/components/layout/theme-toggle.tsx`:
    - 'use client' directive
    - Import useTheme from 'next-themes'
    - Toggle between 'light' and 'dark' via setTheme
    - Render Sun icon when dark (clicking switches to light), Moon icon when light (clicking switches to dark)
    - Use simple SVG icons inline
    - Button styling: p-2 rounded-md text-white/80 hover:text-white, min-h-[44px] min-w-[44px] for touch target

    Create `src/components/layout/top-bar.tsx`:
    - 'use client' directive
    - Import LocaleToggle and ThemeToggle
    - Render a horizontal flex container: `flex items-center gap-2`
    - Order: ThemeToggle, LocaleToggle, then a placeholder div for StaffSwitcher (Phase 5 built this -- it will be wired in during integration)
    - Comment: `{/* StaffSwitcher is rendered here by the dashboard layout -- see 05-03 */}`

    CRITICAL: Import useRouter and usePathname from '@/i18n/navigation' in LocaleToggle, never from 'next/navigation'. This prevents the locale stacking bug (/en/ja/...).
  </action>
  <verify>
    Run: `npm run type-check` -- exits 0.
    Check: `grep "useRouter.*@/i18n/navigation" src/components/layout/locale-toggle.tsx` -- found.
    Check: `grep "router.replace" src/components/layout/locale-toggle.tsx` -- found.
    Check: `grep "useTheme" src/components/layout/theme-toggle.tsx` -- found.
    Check: `grep "LocaleToggle" src/components/layout/top-bar.tsx` -- found.
  </verify>
  <done>
    TopBar renders with locale toggle (EN/JP switch via router.replace) and theme toggle (light/dark via useTheme). Locale toggle imports from @/i18n/navigation. StaffSwitcher slot marked for integration.
  </done>
</task>

</tasks>

<verification>
1. `npm run type-check` exits 0
2. Sidebar uses usePathname from @/i18n/navigation (not next/navigation)
3. LocaleToggle uses router.replace from @/i18n/navigation (not next/navigation)
4. ThemeToggle uses useTheme from next-themes
5. Sidebar has 4 nav items with proper SVG icons
6. All interactive elements have min 44px touch targets
</verification>

<success_criteria>
- Custom 90px sidebar with icon-only navigation and translated labels
- Active page highlighted in sidebar via pathname detection
- Locale toggle switches between EN and JP without URL stacking
- Theme toggle switches between light and dark without flash
- No shadcn Sidebar primitive used (custom implementation per reference app)
- All interactive elements are touch-friendly (44px minimum)
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-polish/06-02-SUMMARY.md`
</output>
