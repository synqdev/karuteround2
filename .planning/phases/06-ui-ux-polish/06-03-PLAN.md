---
phase: 06-ui-ux-polish
plan: "03"
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/app/[locale]/(app)/layout.tsx
  - messages/en.json
  - messages/ja.json
autonomous: true

must_haves:
  truths:
    - "Dashboard layout renders the Sidebar and TopBar components from Plan 06-02 in the layout shell from Plan 06-01"
    - "All UI strings across all pages are in messages/en.json and messages/ja.json with matching keys"
    - "Toggling locale switches all visible UI text immediately -- no raw key strings appear on any page"
    - "Category badge labels (SYMPTOM, TREATMENT, LIFESTYLE, etc.) have translation keys under karute.categories"
    - "Translation namespaces cover: common, auth, sidebar, recording, customers, customerForm, karute, settings"
  artifacts:
    - path: "src/app/[locale]/(app)/layout.tsx"
      provides: "Complete dashboard layout wiring Sidebar + TopBar + StaffSwitcher into the layout shell"
      contains: "Sidebar"
    - path: "messages/en.json"
      provides: "Complete English translation strings for all pages built in Phases 1-5"
      contains: "karute"
    - path: "messages/ja.json"
      provides: "Complete Japanese translation strings for all pages built in Phases 1-5"
      contains: "karute"
  key_links:
    - from: "src/app/[locale]/(app)/layout.tsx"
      to: "src/components/layout/sidebar.tsx"
      via: "Sidebar component rendered in layout flex container"
      pattern: "Sidebar"
    - from: "src/app/[locale]/(app)/layout.tsx"
      to: "src/components/layout/top-bar.tsx"
      via: "TopBar component rendered in header area"
      pattern: "TopBar"
    - from: "messages/en.json"
      to: "messages/ja.json"
      via: "Matching key structure -- every key in en.json must exist in ja.json"
      pattern: "sidebar"
---

<objective>
Wire the sidebar and TopBar into the dashboard layout, then audit and complete all translation files so every UI string is translatable.

Purpose: Plans 06-01 and 06-02 created the layout shell and navigation components separately. This plan integrates them and ensures the bilingual toggle actually works across every page -- not just the pages that had translations from Phase 1.
Output: Fully integrated dashboard layout with sidebar + TopBar, complete EN/JP translation files covering all pages from Phases 1-5.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ui-ux-polish/06-RESEARCH.md
@.planning/phases/06-ui-ux-polish/06-01-SUMMARY.md
@.planning/phases/06-ui-ux-polish/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Sidebar and TopBar into dashboard layout</name>
  <files>
    src/app/[locale]/(app)/layout.tsx
  </files>
  <action>
    Update `src/app/[locale]/(app)/layout.tsx` to wire in the components from Plans 06-01 and 06-02:

    Import Sidebar from '@/components/layout/sidebar'
    Import TopBar from '@/components/layout/top-bar'
    Import StaffSwitcher from '@/components/staff/StaffSwitcher' (built in Phase 5)

    The layout shell structure from Plan 06-01 has placeholder slots. Fill them in:
    ```
    <div className="flex h-screen flex-col bg-[#e8e8e8] p-3 dark:bg-[#2a2a2a]">
      <div className="flex items-center justify-end px-4 py-1">
        <TopBar />
        {/* StaffSwitcher may be inside TopBar or beside it -- check Phase 5's 05-03 implementation */}
      </div>
      <div className="flex flex-1 gap-3 min-h-0">
        <div className="relative">
          <Sidebar />
        </div>
        <main className="relative flex-1 overflow-y-auto rounded-[28px] bg-white dark:bg-[#3a3a3a]">
          <div className="mx-auto max-w-7xl p-6">
            {children}
          </div>
        </main>
      </div>
    </div>
    ```

    Check how StaffSwitcher was wired in Phase 5's plan (05-03). If it was already in this layout, preserve that wiring. If it was in a different location, move it into the TopBar area.

    Ensure NextIntlClientProvider still wraps this layout (it was set up in Phase 1's 01-04).

    Remove any remnants of the old shadcn SidebarProvider/AppSidebar/SidebarTrigger if they still exist in this layout. The custom sidebar replaces them entirely.
  </action>
  <verify>
    Run: `npm run type-check` -- exits 0.
    Run: `npm run dev` -- visit localhost:3000, verify:
    1. 90px sidebar visible on the left with icons
    2. TopBar visible at the top-right with locale and theme toggles
    3. Content area has rounded corners and dark background
    4. Clicking sidebar links navigates between pages
    5. Active page is highlighted in sidebar
    Check: `grep "Sidebar" src/app/\\[locale\\]/\\(app\\)/layout.tsx` -- found.
    Check: `grep "TopBar" src/app/\\[locale\\]/\\(app\\)/layout.tsx` -- found.
  </verify>
  <done>
    Dashboard layout integrates Sidebar, TopBar, and StaffSwitcher. Custom sidebar replaces any previous shadcn sidebar. Layout matches reference app shell pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Complete EN/JP translation files for all pages</name>
  <files>
    messages/en.json
    messages/ja.json
  </files>
  <action>
    Audit every page and component built in Phases 1-5. For each hardcoded string, add a translation key.

    Read all page and component files to find untranslated strings:
    - src/app/[locale]/(app)/sessions/page.tsx (recording page)
    - src/app/[locale]/(app)/customers/page.tsx (customer list)
    - src/app/[locale]/(app)/customers/[id]/page.tsx (customer profile -- if exists)
    - src/app/[locale]/(app)/karute/page.tsx (karute list -- if exists)
    - src/app/[locale]/(app)/karute/[id]/page.tsx (karute detail)
    - src/app/[locale]/(app)/settings/page.tsx (staff management)
    - src/app/[locale]/login/page.tsx (login page)
    - src/components/recording-modal.tsx
    - src/components/staff-selector.tsx
    - src/components/customers/*.tsx
    - src/components/karute/*.tsx
    - src/components/staff/*.tsx
    - src/components/layout/sidebar.tsx (already uses t() from Plan 06-02)

    Update messages/en.json with complete namespace structure:
    ```json
    {
      "common": { "app_name", "loading", "save", "cancel", "delete", "edit", "confirm", "back", "search", "noResults" },
      "auth": { "title", "email", "password", "submit", "submitting", "error_invalid" },
      "sidebar": { "recording", "customers", "karute", "settings" },
      "recording": { "title", "select_staff", "start", "stop", "discard", "use_recording", "recording_in_progress", "idle", "newSession", "recordDescription" },
      "customers": { "title", "new", "search", "name", "contact", "lastVisit", "visits", "noCustomers", "deleteConfirm" },
      "customerForm": { "name", "furigana", "phone", "email", "notes", "create", "update" },
      "karute": { "title", "summary", "transcript", "entries", "addEntry", "noEntries", "categories": { "SYMPTOM", "TREATMENT", "PREFERENCE", "LIFESTYLE", "CONCERN", "OBSERVATION", "RECOMMENDATION" }, "confidence", "staff", "date", "customer" },
      "settings": { "title", "staffManagement", "addStaff", "editStaff", "deleteStaff", "deleteConfirm", "name", "active" }
    }
    ```

    Update messages/ja.json with matching Japanese translations for every key.

    Japanese translations for category badges:
    - SYMPTOM: "症状"
    - TREATMENT: "施術"
    - PREFERENCE: "好み"
    - LIFESTYLE: "ライフスタイル"
    - CONCERN: "懸念"
    - OBSERVATION: "観察"
    - RECOMMENDATION: "提案"

    CRITICAL: Do NOT add translation keys for AI-generated content (transcript text, summary text, entry content). Those are database values displayed as-is. Only translate UI chrome: labels, buttons, headings, placeholders, error messages.

    CRITICAL: Every key in en.json MUST have a corresponding key in ja.json. Missing keys will show raw key strings when locale is toggled.

    After updating translation files, also update any components that still have hardcoded English strings to use useTranslations() or getTranslations(). This means reading each component file, finding hardcoded text, and replacing with t() calls using the appropriate namespace.
  </action>
  <verify>
    Run: `npm run type-check` -- exits 0.
    Run: `node -e "const en = require('./messages/en.json'); const ja = require('./messages/ja.json'); const enKeys = JSON.stringify(Object.keys(en).sort()); const jaKeys = JSON.stringify(Object.keys(ja).sort()); console.log(enKeys === jaKeys ? 'PASS: top-level keys match' : 'FAIL: key mismatch')"` -- PASS.
    Run: `npm run dev` -- toggle locale and verify no raw key strings appear on:
    1. Login page
    2. Sessions/recording page
    3. Customer list page
    4. Karute detail page
    5. Settings page
    6. Sidebar labels
  </verify>
  <done>
    messages/en.json and messages/ja.json have complete, matching translation keys for all namespaces (common, auth, sidebar, recording, customers, customerForm, karute, settings). All components use t() for UI strings. Toggling locale shows translated text on every page.
  </done>
</task>

</tasks>

<verification>
1. `npm run type-check` exits 0
2. Dashboard layout renders Sidebar + TopBar + content area correctly
3. Sidebar navigation works -- clicking items navigates to correct pages
4. Active page is highlighted in sidebar
5. Locale toggle switches all UI text between EN and JP
6. No raw key strings appear on any page after toggling
7. Category badges show translated labels
8. AI-generated content (transcript, summary, entries) is NOT wrapped in t()
</verification>

<success_criteria>
- Sidebar and TopBar are wired into the dashboard layout shell
- Old shadcn SidebarProvider/AppSidebar removed from layout
- Translation files cover all 8 namespaces with matching EN/JP keys
- Locale toggle works on every page without showing raw key strings
- Category badge labels are translated via karute.categories namespace
- No AI output is wrapped in t()
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-polish/06-03-SUMMARY.md`
</output>
