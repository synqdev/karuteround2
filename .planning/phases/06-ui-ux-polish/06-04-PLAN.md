---
phase: 06-ui-ux-polish
plan: "04"
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - src/app/[locale]/(app)/layout.tsx
  - src/components/layout/sidebar.tsx
  - src/components/recording-modal.tsx
  - src/components/customers/CustomerTable.tsx
  - src/components/karute/KaruteDetailView.tsx
  - src/components/staff/StaffForm.tsx
autonomous: false

must_haves:
  truths:
    - "App is fully usable at 1024px viewport width -- no broken layouts, no horizontal scrolling"
    - "All interactive elements (buttons, links, form inputs) have minimum 44x44px touch targets"
    - "Recording modal renders correctly on tablet -- not clipped, not overflowing"
    - "Customer table is readable at 768px -- columns adjust or become scrollable"
    - "Sidebar remains visible and functional at 1024px"
    - "Human verifies the complete UI is visually consistent and functionally correct"
  artifacts:
    - path: "src/app/[locale]/(app)/layout.tsx"
      provides: "Tablet-responsive dashboard layout with proper flex behavior at 1024px"
      contains: "min-h-0"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Sidebar with touch-friendly nav buttons"
      contains: "min-h-[44px]"
  key_links:
    - from: "src/app/[locale]/(app)/layout.tsx"
      to: "src/components/layout/sidebar.tsx"
      via: "Sidebar rendered in flex container that holds shape at 1024px"
      pattern: "flex.*gap-3"
---

<objective>
Make every page comfortable on a tablet viewport and verify the complete Phase 6 UI polish end-to-end.

Purpose: The app is used on shared salon tablets. Layouts that work on a desktop monitor may break at 1024px. This plan fixes responsive issues and includes human verification of the full UI polish.
Output: Tablet-responsive layouts across all pages, touch-friendly controls, human-verified visual quality.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ui-ux-polish/06-RESEARCH.md
@.planning/phases/06-ui-ux-polish/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tablet responsive audit and fixes across all pages</name>
  <files>
    src/app/[locale]/(app)/layout.tsx
    src/components/layout/sidebar.tsx
    src/components/recording-modal.tsx
    src/components/customers/CustomerTable.tsx
    src/components/karute/KaruteDetailView.tsx
    src/components/staff/StaffForm.tsx
  </files>
  <action>
    Audit and fix each component for tablet viewport (768-1024px):

    **Dashboard layout (src/app/[locale]/(app)/layout.tsx):**
    - Verify the flex layout holds at 1024px: sidebar (90px fixed) + content (flex-1) + gap-3 + p-3 = sidebar does not overlap content
    - Content area max-w-7xl may be too wide at 1024px -- ensure it gracefully shrinks
    - Add overflow-hidden on the outer flex container to prevent horizontal scroll

    **Sidebar (src/components/layout/sidebar.tsx):**
    - Already 90px wide which fits at 1024px (leaves ~920px for content)
    - Ensure nav buttons have min-h-[44px] min-w-[44px] for touch targets
    - Verify text labels don't overflow the 90px width at any font size

    **Recording modal (src/components/recording-modal.tsx):**
    - Use shadcn Sheet with side="bottom" at tablet breakpoints if the Dialog is too small for touch interaction
    - OR ensure the Dialog max-w-md is appropriate and the mic button (h-20 w-20) is easily tappable
    - Ensure waveform canvas width is responsive (w-full, not fixed 360px that might overflow)
    - Staff selector buttons need adequate touch spacing

    **Customer table (src/components/customers/CustomerTable.tsx):**
    - At 768px, the table may overflow. Add horizontal scroll wrapper: `<div className="overflow-x-auto">`
    - Alternatively, hide less important columns at smaller breakpoints using responsive display classes
    - Ensure table row tap targets are adequate for touch navigation

    **Karute detail (src/components/karute/KaruteDetailView.tsx):**
    - If using two-column layout (entries + transcript), stack to single column below 1024px: `grid grid-cols-1 lg:grid-cols-2`
    - Entry cards should have adequate padding for touch
    - Add Entry button needs min-h-[44px]

    **Staff form (src/components/staff/StaffForm.tsx):**
    - Form inputs need adequate height for touch: min-h-[44px] on input elements
    - Submit buttons need touch-friendly sizing
    - If using a Sheet/Dialog, ensure it's appropriately sized

    General fixes to apply across all components:
    - Any icon-only button: add min-h-[44px] min-w-[44px] or p-2.5 minimum
    - Any clickable table row: ensure row height is >= 44px
    - Any form input: ensure height is >= 44px (shadcn default h-10 = 40px, may need h-11)
  </action>
  <verify>
    Run: `npm run type-check` -- exits 0.
    Run: `npm run dev` -- resize browser to 1024px width:
    1. Dashboard layout: sidebar + content fit without horizontal scroll
    2. Navigate to each page and verify no layout breakage
    3. Resize to 768px and verify graceful degradation (scrollable tables, stacked columns)
  </verify>
  <done>
    All pages render correctly at 1024px tablet viewport. Touch targets are 44px minimum. Tables are scrollable. Two-column layouts stack appropriately. No horizontal scrolling on the main layout.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify -- complete UI/UX polish</name>
  <files>
    src/app/[locale]/(app)/layout.tsx
    src/components/layout/sidebar.tsx
    src/components/layout/top-bar.tsx
  </files>
  <action>Human verifies the complete Phase 6 UI/UX polish works end-to-end: dark theme, sidebar navigation, locale toggle, theme toggle, bilingual translations, tablet responsiveness, and touch-friendly controls.</action>
  <verify>All 12 verification steps in how-to-verify pass without issues.</verify>
  <done>Human has approved the complete UI polish. All pages are visually consistent, fully bilingual, navigable via sidebar, and comfortable on a tablet.</done>
  <what-built>
    Complete Phase 6 UI/UX polish: dark theme with reference app aesthetic (#2a2a2a/#3a3a3a/#4a4a4a layout shell), custom 90px sidebar with active state, TopBar with locale and theme toggles, complete EN/JP translations for all pages, tablet-responsive layouts with touch-friendly controls, Inter + Noto Sans JP bilingual font stack.
  </what-built>
  <how-to-verify>
    1. Open the app at http://localhost:3000 in a browser
    2. DARK THEME: Verify the app loads in dark mode with no flash of light theme
    3. LAYOUT SHELL: Verify the overall layout matches the mockup aesthetic:
       - Dark outer background (#2a2a2a feel)
       - Slightly lighter content area with rounded corners
       - 90px sidebar on the left with icons
    4. SIDEBAR: Verify sidebar shows 4 nav items (Recording, Customers, Karute, Settings) with icons
    5. SIDEBAR ACTIVE STATE: Click each nav item -- verify the active page is highlighted (white text) while others are dimmed
    6. LOCALE TOGGLE: Click the locale toggle in the top bar
       - Verify ALL UI text switches between English and Japanese
       - Check sidebar labels change
       - Check page headings change
       - Check button labels change
       - Check that NO raw key strings (like "customers.title") appear anywhere
       - Verify AI-generated content (if any visible) does NOT change when toggling
    7. THEME TOGGLE: Click the theme toggle -- verify smooth switch to light mode and back to dark
    8. FONTS: Type or view Japanese text -- verify it renders in Noto Sans JP (consistent weight, not a fallback CJK font)
    9. TABLET: Resize browser to 1024px width
       - Verify sidebar + content fit without horizontal scrolling
       - Navigate to each page -- no layout breakage
       - Verify buttons and interactive elements are easy to tap (not too small)
    10. TABLET 768px: Resize to 768px
       - Verify graceful degradation (tables scroll, columns stack)
       - Recording modal is still usable
    11. CATEGORY BADGES (on karute detail page): Verify badges show translated labels (not enum keys) in both languages
    12. Check browser console for any errors or warnings
  </how-to-verify>
  <resume-signal>Type "approved" when the UI polish is complete and consistent, or describe any issues that need fixing.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run type-check` exits 0
2. App loads in dark mode without flash at any viewport width
3. Sidebar navigation works with correct active state
4. Locale toggle switches all UI text (no raw keys visible)
5. Theme toggle works without flash
6. Layout holds at 1024px -- no horizontal scroll
7. Touch targets are 44px minimum on all interactive elements
8. Human verified: complete UI is visually consistent and functionally correct
</verification>

<success_criteria>
- Every page uses dark theme with visual consistency matching the mockup
- User can toggle language and all UI text switches immediately
- Sidebar navigation links to all pages with active state
- App is fully usable on a tablet viewport (1024px) with touch-friendly controls
- Human has verified the complete UI polish end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-polish/06-04-SUMMARY.md`
</output>
