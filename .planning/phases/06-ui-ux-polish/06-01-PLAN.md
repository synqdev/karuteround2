---
phase: 06-ui-ux-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/providers/theme-provider.tsx
  - src/app/[locale]/(app)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "App loads in dark mode by default with no flash of light theme"
    - "Root layout uses ThemeProvider with attribute='class', defaultTheme='dark', enableSystem=false"
    - "html tag has suppressHydrationWarning attribute"
    - "Inter and Noto Sans JP fonts are loaded via next/font with correct subsets and weights"
    - "Dashboard layout uses the reference app shell: outer bg-[#2a2a2a] p-3, content bg-[#3a3a3a] rounded-[28px], sidebar bg-[#4a4a4a]"
    - "globals.css uses OKLCH color values in @theme inline with @custom-variant dark"
  artifacts:
    - path: "src/components/providers/theme-provider.tsx"
      provides: "next-themes wrapper with class-based dark mode"
      contains: "NextThemesProvider"
    - path: "src/app/layout.tsx"
      provides: "Root layout with ThemeProvider, Inter + Noto Sans JP fonts, suppressHydrationWarning"
      contains: "suppressHydrationWarning"
    - path: "src/app/[locale]/(app)/layout.tsx"
      provides: "Dashboard layout shell matching reference app with sidebar slot, TopBar slot, rounded content area"
      contains: "bg-[#2a2a2a]"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme inline tokens with OKLCH dark mode values"
      contains: "@custom-variant dark"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/providers/theme-provider.tsx"
      via: "ThemeProvider wraps body children"
      pattern: "ThemeProvider"
    - from: "src/app/globals.css"
      to: "Tailwind v4"
      via: "@theme inline wires CSS vars into Tailwind namespace"
      pattern: "@theme inline"
---

<objective>
Establish the visual foundation: dark theme infrastructure (ThemeProvider, globals.css tokens, fonts) and the dashboard layout shell matching the reference app aesthetic.

Purpose: Every other plan in Phase 6 builds on this layout shell. The sidebar, TopBar, and page content all render inside this structure. Getting the dark theme tokens, font stack, and layout skeleton right first prevents rework.
Output: Root layout with ThemeProvider + bilingual fonts, dashboard layout shell with the reference app's #2a2a2a/#3a3a3a/#4a4a4a color scheme, globals.css with OKLCH dark tokens.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-ux-polish/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ThemeProvider, bilingual fonts, and root layout overhaul</name>
  <files>
    src/components/providers/theme-provider.tsx
    src/app/layout.tsx
  </files>
  <action>
    Create `src/components/providers/theme-provider.tsx`:
    - 'use client' directive
    - Import ThemeProvider as NextThemesProvider from 'next-themes'
    - Export ThemeProvider component wrapping children in NextThemesProvider
    - Props: attribute="class", defaultTheme="dark", enableSystem={false}
    - Match the exact pattern from the research (synqdev/karute reference)

    Update `src/app/layout.tsx` (root layout):
    - Add suppressHydrationWarning to the html tag (required for next-themes)
    - Configure Inter font: subsets ['latin'], variable '--font-inter'
    - Configure Noto_Sans_JP font: subsets ['latin', 'japanese'], weight ['400', '500', '700'], variable '--font-noto-sans-jp'
    - Apply both font variables to body className: `${inter.variable} ${notoSansJP.variable} font-sans antialiased`
    - Wrap body children in ThemeProvider (from the new providers file)
    - Add Toaster from sonner OUTSIDE ThemeProvider, at body level
    - Remove any existing hardcoded `className="dark"` on html (ThemeProvider handles this now)
    - Keep html lang="en" (next-intl handles per-locale lang via [locale] layout)
    - Do NOT put NextIntlClientProvider or SidebarProvider here -- those stay in [locale] layout

    CRITICAL: ThemeProvider must be in root layout, NOT inside [locale] layout. This ensures theme works on login page and all routes.
    CRITICAL: Import fonts from 'next/font/google', NOT from a CDN link tag.
  </action>
  <verify>
    Run: `npm run type-check` -- exits 0.
    Check: `grep "suppressHydrationWarning" src/app/layout.tsx` -- found.
    Check: `grep "ThemeProvider" src/app/layout.tsx` -- found.
    Check: `grep "Noto_Sans_JP" src/app/layout.tsx` -- found (font import).
    Check: `grep "NextThemesProvider" src/components/providers/theme-provider.tsx` -- found.
  </verify>
  <done>
    Root layout has ThemeProvider with class-based dark mode, Inter + Noto Sans JP fonts loaded via next/font, suppressHydrationWarning on html. No theme flash on load.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dark theme tokens in globals.css and dashboard layout shell</name>
  <files>
    src/app/globals.css
    src/app/[locale]/(app)/layout.tsx
  </files>
  <action>
    Update `src/app/globals.css`:
    - Ensure file starts with `@import "tailwindcss";` and `@import "tw-animate-css";`
    - Ensure `@custom-variant dark (&:where(.dark, .dark *));` is present after imports
    - Ensure `@theme inline` block wires CSS vars into Tailwind namespace, including:
      - --color-background, --color-foreground, --color-card, --color-card-foreground
      - --color-primary, --color-primary-foreground, --color-muted, --color-muted-foreground
      - --color-border, --color-input, --color-ring, --color-destructive
      - --color-sidebar-background, --color-sidebar-foreground, --color-sidebar-border
      - --radius-lg: var(--radius)
      - --font-sans: var(--font-inter), var(--font-noto-sans-jp), ui-sans-serif, system-ui
    - Ensure :root has light mode values using OKLCH (for completeness, though dark is default)
    - Ensure .dark selector has OKLCH overrides matching reference app aesthetic:
      - --background: oklch(0.145 0 0)
      - --foreground: oklch(0.985 0 0)
      - --card: oklch(0.205 0 0)
      - --border: oklch(1 0 0 / 10%)
      - --input: oklch(1 0 0 / 15%)
      - Other shadcn tokens as needed for dark theme consistency
    - Do NOT remove any existing shadcn token definitions -- only verify and fix values

    Update `src/app/[locale]/(app)/layout.tsx` (dashboard layout):
    - Restructure to match the reference app's layout shell pattern:
      ```
      <div className="flex h-screen flex-col bg-[#e8e8e8] p-3 dark:bg-[#2a2a2a]">
        <div className="flex items-center justify-end px-4 py-1">
          {/* TopBar slot -- Plan 06-02 will add TopBar component here */}
        </div>
        <div className="flex flex-1 gap-3 min-h-0">
          <div className="relative">
            {/* Sidebar slot -- Plan 06-02 will add Sidebar component here */}
          </div>
          <main className="relative flex-1 overflow-y-auto rounded-[28px] bg-white dark:bg-[#3a3a3a]">
            <div className="mx-auto max-w-7xl p-6">
              {children}
            </div>
          </main>
        </div>
      </div>
      ```
    - Keep NextIntlClientProvider wrapping from the existing locale layout
    - Remove any existing SidebarProvider/shadcn sidebar usage from this layout -- Plan 06-02 replaces it with the custom sidebar
    - Leave slot comments for TopBar and Sidebar that Plan 06-02 will fill in
    - The outer div uses hardcoded hex for layout shell (#2a2a2a, #3a3a3a) per reference app pattern
    - Content inside the main area should use shadcn tokens (bg-card, text-foreground, etc.)
  </action>
  <verify>
    Run: `npm run type-check` -- exits 0.
    Run: `npm run dev` -- visit localhost:3000, verify dark background renders without white flash.
    Check: `grep "@custom-variant dark" src/app/globals.css` -- found.
    Check: `grep "bg-\\[#2a2a2a\\]" src/app/\\[locale\\]/\\(app\\)/layout.tsx` -- found.
    Check: `grep "rounded-\\[28px\\]" src/app/\\[locale\\]/\\(app\\)/layout.tsx` -- found (content area).
  </verify>
  <done>
    globals.css has complete dark theme tokens with OKLCH values. Dashboard layout shell matches the reference app: outer #2a2a2a, content #3a3a3a with rounded-[28px], sidebar and TopBar slots ready for Plan 06-02.
  </done>
</task>

</tasks>

<verification>
1. `npm run type-check` exits 0
2. `npm run dev` -- app loads in dark mode without any flash of light theme
3. `grep "suppressHydrationWarning" src/app/layout.tsx` -- found
4. `grep "@custom-variant dark" src/app/globals.css` -- found
5. `grep "bg-\\[#2a2a2a\\]" src/app/\\[locale\\]/\\(app\\)/layout.tsx` -- found
6. `grep "Noto_Sans_JP" src/app/layout.tsx` -- found
7. ThemeProvider is in root layout, NOT in [locale] layout
</verification>

<success_criteria>
- Dark mode loads by default with no flash (ThemeProvider + suppressHydrationWarning working)
- Inter + Noto Sans JP fonts render correctly for both English and Japanese text
- Dashboard layout shell matches the reference app structure: padded outer wrapper, rounded content area, sidebar slot
- globals.css uses Tailwind v4 CSS-first pattern with @theme inline and OKLCH values
- No tailwind.config.js exists (CSS-first confirmed)
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-polish/06-01-SUMMARY.md`
</output>
